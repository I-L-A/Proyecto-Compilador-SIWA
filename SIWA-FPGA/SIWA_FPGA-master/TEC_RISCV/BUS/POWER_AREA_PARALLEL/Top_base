`timescale 1ns / 1ps
`define ALL_BDSCT 0
`define PCKG_SZ _pckgsz_ 
`define BROADCAST {8{1'b1}}
`define DRVRS _drvrs_ 
`define BITS 1       //Here this mean the number of buses. 
`include "../../Library.sv"

module top_del_sistema(
 //inputs
  input clk,
  input reset,
  input pndng[`BITS-1:0][`DRVRS-1:0],
  input [`PCKG_SZ-1:0] D_pop[`BITS-1:0][`DRVRS-1:0],
 //outputs
  output push[`BITS-1:0][`DRVRS-1:0],
  output pop[`BITS-1:0][`DRVRS-1:0],
  output [`PCKG_SZ-1:0] D_push[`BITS-1:0][`DRVRS-1:0]
);

 prll_bs_gnrtr_n_rbtr #(.buses(`BITS),.bits(`PCKG_SZ),.drvrs(`DRVRS),.broadcast(`BROADCAST)) uut(
  .clk(clk),
  .reset(reset),
  .pndng(pndng),
  .push(push),
  .pop(pop),
  .D_pop(D_pop),
  .D_push(D_push)
);


endmodule
