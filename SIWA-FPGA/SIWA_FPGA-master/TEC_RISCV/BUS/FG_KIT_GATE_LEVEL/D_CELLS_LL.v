`include "VLG_PRIMITIVES.v"
//************************************************************************************
// Copyright        : (c) All Rights Reserved
// Company          : X-FAB Semiconductor Foundries AG
// Address          : Haarbergstr. 67,  D-99097 Erfurt, Germany
//
// File             : D_CELLS_LL.v
// Description      : Verilog model file for Library D_CELLS_LL
//                  : with negative timing check support; xt018, Digital
//                    Logic Library, 1.8V, 1.2V Low Leakage & Low Power
//
// Technology       : 0.18um HV SOI CMOS
// Lib_version      : 2.0.0, Wed Aug 10 12:47:20 2016
// Created by       : X-FAB Library Characterizer XLICDD 
// Program version  : XLICDD - waz2_gvt_main, 2.1000 build 9 Time stamp:
//                    Tue Mar  3 15:18:20 2016
// Last Modified by : XLICDD generated
//
// #######################################################################
//
//  Copyright (c) X-FAB Semiconductor Foundries AG . All rights reserved.
//  This Design Kit data and the associated documentation are
//  confidential and proprietary to X-FAB Semiconductor Foundries AG.
//
//  DISCLAIMER
//  The information contained herein is provided by X-FAB Semiconductor
//  Foundries AG  on an "AS IS" basis without any warranty.
//
//  X-FAB Semiconductor Foundries AG disclaims any representation that
//  the information does not infringe any intellectual property rights or 
//  proprietary rights of any third parties. There are no other warranties 
//  given by X-FAB, whether express, implied or statutory, including, 
//  without limitation, implied warranties of merchantability and fitness 
//  for a particular purpose.
//
//  In no event X-FAB Semiconductor Foundries AG will be liable or 
//  responsible for any expense, losses, damages, or action incurred or 
//  undertaken as a result of the receipt of the information.
// 
//  X-FAB Semiconductor Foundries AG  reserves the right to make changes
//  to the information at any time and without notice.
//
// #######################################################################
// ***********************************************************************
// Remark:
// 
// This Verilog library uses  User Defined Primitives (UDP)
// which are defined in the Verilog library file VLG_PRIMITIVES.v.
//
// If an ATPG tool is used then the compiler directive 
//   `define ATPG_RUN yes
// has to be set before reading this library.
// 
// Use the compiler directive command
//   `define  DISPLAY_HOLD yes
// to enable the logic pullup/down behaviour of the Leakage current 
// compensator cell SIGNALHOLD* (see also the comment in the SIGNALHOLD* 
// cell model)
// 
// Use the compiler directive command
//   `define  NEG_TCHK yes
// to enable the syntax support for negative timing checks. 
//
//************************************************************************/
 

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN211LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN211LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C+D)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN211LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN211LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C+D)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN211LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN211LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C+D)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN211LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN211LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C+D)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN21LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN21LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN21LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN21LLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN21LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN21LLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN21LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN21LLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN21SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN21SLLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN21SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN21SLLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN21SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN21SLLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN21SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN21SLLX6 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A*B)+C)
   and       i0  (n_0, A, B);
   nor       i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN221LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN221LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B)+(C*D)+E)
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0) || (B == 1'b0 && C == 1'b0 && D ==
                   1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN221LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN221LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B)+(C*D)+E)
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0) || (B == 1'b0 && C == 1'b0 && D ==
                   1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN221LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN221LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B)+(C*D)+E)
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0) || (B == 1'b0 && C == 1'b0 && D ==
                   1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN221LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN221LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire  n_0, n_1;

// Function Q: !((A*B)+(C*D)+E)
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0) || (B == 1'b0 && C == 1'b0 && D ==
                   1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN222LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN222LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire  n_0, n_1, n_2;

// Function Q: !((A*B)+(C*D)+(E*F))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   and       i2  (n_2, E, F);
   nor       i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (C -=> Q) =
                   (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (D -=> Q) =
                   (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E -=> Q) =
                   (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (F -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN222LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN222LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire  n_0, n_1, n_2;

// Function Q: !((A*B)+(C*D)+(E*F))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   and       i2  (n_2, E, F);
   nor       i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (C -=> Q) =
                   (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (D -=> Q) =
                   (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E -=> Q) =
                   (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (F -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN222LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN222LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire  n_0, n_1, n_2;

// Function Q: !((A*B)+(C*D)+(E*F))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   and       i2  (n_2, E, F);
   nor       i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (C -=> Q) =
                   (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (D -=> Q) =
                   (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E -=> Q) =
                   (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (F -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN222LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN222LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire  n_0, n_1, n_2;

// Function Q: !((A*B)+(C*D)+(E*F))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   and       i2  (n_2, E, F);
   nor       i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (C -=> Q) =
                   (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (D -=> Q) =
                   (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E -=> Q) =
                   (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (F -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN22LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN22LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire  n_0, n_1;

// Function Q: !((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN22LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN22LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q; 
   wire n_0, n_1;

// Function Q: !((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN22LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN22LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN22LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN22LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN22SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN22SLLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN22SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN22SLLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN22SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN22SLLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN22SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN22SLLX6 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN311LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN311LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D+E)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN311LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN311LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D+E)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN311LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN311LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D+E)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN311LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN311LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D+E)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN31LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN31LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN31LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN31LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN31LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN31LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN31LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN31LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN31SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN31SLLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN31SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN31SLLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN31SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN31SLLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN31SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AN31SLLX6 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   nor       i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN321LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, 2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN321LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E)+F)
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   nor       i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1) || (B == 1'b0 && C == 1'b1 && D == 1'b1
                   && E == 1'b0) || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D
                   == 1'b0 && E == 1'b0) || (A == 1'b0 && B == 1'b1 && C ==
                   1'b1 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b1 && E == 1'b0)
                   || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0 && E
                   == 1'b1)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1))
                   (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN321LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, 2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN321LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E)+F)
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   nor       i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1) || (B == 1'b0 && C == 1'b1 && D == 1'b1
                   && E == 1'b0) || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D
                   == 1'b0 && E == 1'b0) || (A == 1'b0 && B == 1'b1 && C ==
                   1'b1 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b1 && E == 1'b0)
                   || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0 && E
                   == 1'b1)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1))
                   (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN321LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, 2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN321LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E)+F)
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   nor       i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1) || (B == 1'b0 && C == 1'b1 && D == 1'b1
                   && E == 1'b0) || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D
                   == 1'b0 && E == 1'b0) || (A == 1'b0 && B == 1'b1 && C ==
                   1'b1 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b1 && E == 1'b0)
                   || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0 && E
                   == 1'b1)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1))
                   (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN321LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, 2-Input AND into 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN321LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E)+F)
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   nor       i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1) || (B == 1'b0 && C == 1'b1 && D == 1'b1
                   && E == 1'b0) || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D
                   == 1'b0 && E == 1'b0) || (A == 1'b0 && B == 1'b1 && C ==
                   1'b1 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b1 && E == 1'b0)
                   || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0 && E
                   == 1'b1)) (F -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1))
                   (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN32LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input, 2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN32LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1) || (A == 1'b1 && B == 1'b1
                   && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1) || (A == 1'b1 && B == 1'b0
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN32LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input, 2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN32LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1) || (A == 1'b1 && B == 1'b1
                   && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1) || (A == 1'b1 && B == 1'b0
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN32LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input, 2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN32LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1) || (A == 1'b1 && B == 1'b1
                   && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1) || (A == 1'b1 && B == 1'b0
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN32LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input, 2-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN32LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1) || (A == 1'b1 && B == 1'b1
                   && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1) || (A == 1'b1 && B == 1'b0
                   && C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN33LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN33LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E*F))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E, F);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN33LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN33LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E*F))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E, F);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN33LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN33LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E*F))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E, F);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AN33LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input AND into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module AN33LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: !((A*B*C)+(D*E*F))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E, F);
   nor       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND2LLX0 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A*B)
   and       i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND2LLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A*B)
   and       i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND2LLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A*B)
   and       i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND2LLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A*B)
   and       i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND2SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AND2SLLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A*B)
   and       i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND2SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AND2SLLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A*B)
   and       i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND2SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AND2SLLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A*B)
   and       i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND2SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AND2SLLX6 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A*B)
   and       i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND3LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A*B*C)
   and       i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND3LLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A*B*C)
   and       i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND3LLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A*B*C)
   and       i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND3LLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A*B*C)
   and       i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND3SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AND3SLLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A*B*C)
   and       i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND3SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AND3SLLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A*B*C)
   and       i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND3SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AND3SLLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A*B*C)
   and       i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND3SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AND3SLLX6 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A*B*C)
   and       i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND4LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND4LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A*B*C*D)
   and       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND4LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND4LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A*B*C*D)
   and       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND4LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND4LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A*B*C*D)
   and       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND4LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND4LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A*B*C*D)
   and       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND4SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input AND with 4-stocked n-channel transistors
//   last modified by : XLICDD generated
//****************************************************************************

module AND4SLLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A*B*C*D)
   and       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND4SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input AND with 4-stocked n-channel transistors
//   last modified by : XLICDD generated
//****************************************************************************

module AND4SLLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A*B*C*D)
   and       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND4SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input AND with 4-stocked n-channel transistors
//   last modified by : XLICDD generated
//****************************************************************************

module AND4SLLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A*B*C*D)
   and       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND4SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input AND with 4-stocked n-channel transistors
//   last modified by : XLICDD generated
//****************************************************************************

module AND4SLLX6 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A*B*C*D)
   and       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND5LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND5LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: (A*B*C*D*E)
   and       i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND5LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND5LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: (A*B*C*D*E)
   and       i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND5LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND5LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: (A*B*C*D*E)
   and       i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND5LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND5LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: (A*B*C*D*E)
   and       i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND6LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND6LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: (A*B*C*D*E*F)
   and       i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND6LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND6LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: (A*B*C*D*E*F)
   and       i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND6LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND6LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: (A*B*C*D*E*F)
   and       i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AND6LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module AND6LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: (A*B*C*D*E*F)
   and       i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO211LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO211LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C+D)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO211LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO211LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C+D)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO211LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO211LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C+D)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO211LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO211LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C+D)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO21LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO21LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO21LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO21LLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO21LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO21LLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO21LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO21LLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO21SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO21SLLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO21SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO21SLLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO21SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO21SLLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO21SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO21SLLX6 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A*B)+C)
   and       i0  (n_0, A, B);
   or        i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO221LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO221LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D)+E)
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0) || (B == 1'b0 && C == 1'b0 && D ==
                   1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO221LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO221LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D)+E)
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0) || (B == 1'b0 && C == 1'b0 && D ==
                   1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO221LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO221LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D)+E)
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0) || (B == 1'b0 && C == 1'b0 && D ==
                   1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO221LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO221LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D)+E)
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0) || (B == 1'b0 && C == 1'b0 && D ==
                   1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO222LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO222LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1,n_2;

// Function Q: ((A*B)+(C*D)+(E*F))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   and       i2  (n_2, E, F);
   or        i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (C +=> Q) =
                   (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (D +=> Q) =
                   (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E +=> Q) =
                   (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (F +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO222LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO222LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1,n_2;

// Function Q: ((A*B)+(C*D)+(E*F))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   and       i2  (n_2, E, F);
   or        i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (C +=> Q) =
                   (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (D +=> Q) =
                   (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E +=> Q) =
                   (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (F +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO222LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO222LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1,n_2;

// Function Q: ((A*B)+(C*D)+(E*F))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   and       i2  (n_2, E, F);
   or        i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (C +=> Q) =
                   (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (D +=> Q) =
                   (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E +=> Q) =
                   (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (F +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO222LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO222LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1,n_2;

// Function Q: ((A*B)+(C*D)+(E*F))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   and       i2  (n_2, E, F);
   or        i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (C +=> Q) =
                   (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1) || (A == 1'b1 && B == 1'b0 && F == 1'b1)) (D +=> Q) =
                   (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (E +=> Q) =
                   (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b0 && C == 1'b1
                   && D == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && D == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b0 && D == 1'b1)) (F +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO22LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO22LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO22LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO22LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO22LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO22LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO22LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO22LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO22SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO22SLLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO22SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO22SLLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO22SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO22SLLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO22SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO22SLLX6 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B)+(C*D))
   and       i0  (n_0, A, B);
   and       i1  (n_1, C, D);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO311LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO311LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D+E)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO311LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO311LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D+E)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO311LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO311LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D+E)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO311LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO311LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D+E)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO31LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO31LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO31LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO31LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO31LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO31LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO31LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO31LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO31SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO31SLLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO31SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO31SLLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO31SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO31SLLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO31SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND into 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module AO31SLLX6 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A*B*C)+D)
   and       i0  (n_0, A, B, C);
   or        i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO321LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, 2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO321LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E)+F)
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   or        i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1) || (B == 1'b0 && C == 1'b1 && D == 1'b1
                   && E == 1'b0) || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D
                   == 1'b0 && E == 1'b0) || (A == 1'b0 && B == 1'b1 && C ==
                   1'b1 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b1 && E == 1'b0)
                   || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0 && E
                   == 1'b1)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1))
                   (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO321LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, 2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO321LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E)+F)
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   or        i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1) || (B == 1'b0 && C == 1'b1 && D == 1'b1
                   && E == 1'b0) || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D
                   == 1'b0 && E == 1'b0) || (A == 1'b0 && B == 1'b1 && C ==
                   1'b1 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b1 && E == 1'b0)
                   || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0 && E
                   == 1'b1)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1))
                   (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO321LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, 2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO321LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E)+F)
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   or        i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1) || (B == 1'b0 && C == 1'b1 && D == 1'b1
                   && E == 1'b0) || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D
                   == 1'b0 && E == 1'b0) || (A == 1'b0 && B == 1'b1 && C ==
                   1'b1 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b1 && E == 1'b0)
                   || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0 && E
                   == 1'b1)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1))
                   (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO321LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input AND, 2-Input AND into 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO321LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E)+F)
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   or        i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b0))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0) || (A == 1'b0 && B == 1'b0
                   && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1) || (A == 1'b0 && B == 1'b1
                   && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1) || (B == 1'b0 && C == 1'b1 && D == 1'b1
                   && E == 1'b0) || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D
                   == 1'b0 && E == 1'b0) || (A == 1'b0 && B == 1'b1 && C ==
                   1'b1 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b1 && E == 1'b0)
                   || (A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0 && E
                   == 1'b1)) (F +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1))
                   (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO32LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input, 2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO32LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1) || (A == 1'b1 && B == 1'b1
                   && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1) || (A == 1'b1 && B == 1'b0
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO32LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input, 2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO32LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1) || (A == 1'b1 && B == 1'b1
                   && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1) || (A == 1'b1 && B == 1'b0
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO32LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input, 2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO32LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1) || (A == 1'b1 && B == 1'b1
                   && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1) || (A == 1'b1 && B == 1'b0
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO32LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input, 2-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO32LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((E == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1) || (A == 1'b1 && B == 1'b1
                   && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1) || (A == 1'b1 && B == 1'b0
                   && C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && C == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1 && C == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO33LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO33LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E*F))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E, F);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO33LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO33LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E*F))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E, F);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO33LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO33LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E*F))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E, F);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : AO33LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input AND into 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module AO33LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0, n_1;

// Function Q: ((A*B*C)+(D*E*F))
   and       i0  (n_0, A, B, C);
   and       i1  (n_1, D, E, F);
   or        i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1 && D == 1'b0 && E == 1'b1 && F == 1'b1))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b1)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHCLLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module BTHCLLX8 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHCLLX12
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module BTHCLLX12 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHCLLX16
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module BTHCLLX16 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHCLLX20
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module BTHCLLX20 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTHLLX0 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTHLLX1 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTHLLX2 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHLLX3
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTHLLX3 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTHLLX4 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTHLLX6 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHLLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTHLLX8 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHLLX12
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTHLLX12 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHLLX16
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTHLLX16 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTHLLX20
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTHLLX20 (A, E, Q);

   input     A, E;
   output    Q;

// Function Q: A; Tristate function: !E
   bufif1    i1  (Q, A, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLCLLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module BTLCLLX8 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLCLLX12
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module BTLCLLX12 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLCLLX16
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module BTLCLLX16 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLCLLX20
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module BTLCLLX20 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTLLLX0 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTLLLX1 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTLLLX2 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLLLX3
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTLLLX3 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTLLLX4 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTLLLX6 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLLLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTLLLX8 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLLLX12
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTLLLX12 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLLLX16
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTLLLX16 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BTLLLX20
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Buffer with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module BTLLLX20 (A, EN, Q);

   input     A, EN;
   output    Q;

// Function Q: A; Tristate function: EN
   bufif0    i0  (Q, A, EN);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BULLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module BULLX0 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BULLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module BULLX1 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BULLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module BULLX2 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BULLX3
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module BULLX3 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BULLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module BULLX4 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BULLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module BULLX6 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BULLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module BULLX8 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BULLX12
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module BULLX12 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BULLX16
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module BULLX16 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : BULLX20
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module BULLX20 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : CAGLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Carry Generator
//   last modified by : XLICDD generated
//****************************************************************************

module CAGLLX0 (A, B, CI, CO);

   input     A, B, CI;
   output    CO;
   wire n_0, n_1, n_2;

// Function CO: (B*CI) + (A*CI) + (A*B)
   and       i0  (n_0, B, CI);
   and       i1  (n_1, A, CI);
   and       i2  (n_2, A, B);
   or        i3  (CO, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);
      (CI +=> CO) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : CAGLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Carry Generator
//   last modified by : XLICDD generated
//****************************************************************************

module CAGLLX1 (A, B, CI, CO);

   input     A, B, CI;
   output    CO;
   wire n_0, n_1, n_2;

// Function CO: (B*CI) + (A*CI) + (A*B)
   and       i0  (n_0, B, CI);
   and       i1  (n_1, A, CI);
   and       i2  (n_2, A, B);
   or        i3  (CO, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);
      (CI +=> CO) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : CAGLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Carry Generator
//   last modified by : XLICDD generated
//****************************************************************************

module CAGLLX2 (A, B, CI, CO);

   input     A, B, CI;
   output    CO;
   wire n_0, n_1, n_2;

// Function CO: (B*CI) + (A*CI) + (A*B)
   and       i0  (n_0, B, CI);
   and       i1  (n_1, A, CI);
   and       i2  (n_2, A, B);
   or        i3  (CO, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);
      (CI +=> CO) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : CAGLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Carry Generator
//   last modified by : XLICDD generated
//****************************************************************************

module CAGLLX4 (A, B, CI, CO);

   input     A, B, CI;
   output    CO;
   wire n_0, n_1, n_2;

// Function CO: (B*CI) + (A*CI) + (A*B)
   and       i0  (n_0, B, CI);
   and       i1  (n_1, A, CI);
   and       i2  (n_2, A, B);
   or        i3  (CO, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);
      (CI +=> CO) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFFQLLX0 (CN, D, Q);

   input     CN, D;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, d_clock_i;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, CN);
   u1_fd5    i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFFQLLX1 (CN, D, Q);

   input     CN, D;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, d_clock_i;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, CN);
   u1_fd5    i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFFQLLX2 (CN, D, Q);

   input     CN, D;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, d_clock_i;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, CN);
   u1_fd5    i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFFQLLX4 (CN, D, Q);

   input     CN, D;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, d_clock_i;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, CN);
   u1_fd5    i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRQLLX0 (CN, D, Q, RN);

   input     CN, D, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, RN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRQLLX1 (CN, D, Q, RN);

   input     CN, D, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, RN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRQLLX2 (CN, D, Q, RN);

   input     CN, D, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, RN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRQLLX4 (CN, D, Q, RN);

   input     CN, D, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, RN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRSQLLX0 (CN, D, Q, RN, SN);

   input     CN, D, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, delay_SN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i3  (IQ, delay_D, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i3  (IQ, D, clock_i, RN, SN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRSQLLX1 (CN, D, Q, RN, SN);

   input     CN, D, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, delay_SN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i3  (IQ, delay_D, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i3  (IQ, D, clock_i, RN, SN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRSQLLX2 (CN, D, Q, RN, SN);

   input     CN, D, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, delay_SN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i3  (IQ, delay_D, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i3  (IQ, D, clock_i, RN, SN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRSQLLX4 (CN, D, Q, RN, SN);

   input     CN, D, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, delay_SN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i3  (IQ, delay_D, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i3  (IQ, D, clock_i, RN, SN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRSLLX0 (CN, D, Q, QN, RN, SN);

   input     CN, D, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, delay_SN, d_clock_i, IQN,
                   qin_and_set, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i3  (IQ, delay_D, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   and       i5  (qin_and_set, IQN, delay_SN);
   buf       i6  (Q, IQ);
   buf       i7  (QN, qin_and_set);

   checkrs   i8  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, qin_and_set, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i3  (IQ, D, clock_i, RN, SN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   and       i5  (qin_and_set, IQN, SN);
   buf       i6  (Q, IQ);
   buf       i7  (QN, qin_and_set);

   checkrs   i8  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRSLLX1 (CN, D, Q, QN, RN, SN);

   input     CN, D, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, delay_SN, d_clock_i, IQN,
                   qin_and_set, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i3  (IQ, delay_D, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   and       i5  (qin_and_set, IQN, delay_SN);
   buf       i6  (Q, IQ);
   buf       i7  (QN, qin_and_set);

   checkrs   i8  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, qin_and_set, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i3  (IQ, D, clock_i, RN, SN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   and       i5  (qin_and_set, IQN, SN);
   buf       i6  (Q, IQ);
   buf       i7  (QN, qin_and_set);

   checkrs   i8  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRSLLX2 (CN, D, Q, QN, RN, SN);

   input     CN, D, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, delay_SN, d_clock_i, IQN,
                   qin_and_set, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i3  (IQ, delay_D, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   and       i5  (qin_and_set, IQN, delay_SN);
   buf       i6  (Q, IQ);
   buf       i7  (QN, qin_and_set);

   checkrs   i8  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, qin_and_set, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i3  (IQ, D, clock_i, RN, SN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   and       i5  (qin_and_set, IQN, SN);
   buf       i6  (Q, IQ);
   buf       i7  (QN, qin_and_set);

   checkrs   i8  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRSLLX4 (CN, D, Q, QN, RN, SN);

   input     CN, D, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, delay_SN, d_clock_i, IQN,
                   qin_and_set, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i3  (IQ, delay_D, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   and       i5  (qin_and_set, IQN, delay_SN);
   buf       i6  (Q, IQ);
   buf       i7  (QN, qin_and_set);

   checkrs   i8  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, qin_and_set, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i3  (IQ, D, clock_i, RN, SN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   and       i5  (qin_and_set, IQN, SN);
   buf       i6  (Q, IQ);
   buf       i7  (QN, qin_and_set);

   checkrs   i8  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRLLX0 (CN, D, Q, QN, RN);

   input     CN, D, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, d_clock_i, IQN, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, RN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRLLX1 (CN, D, Q, QN, RN);

   input     CN, D, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, d_clock_i, IQN, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, RN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRLLX2 (CN, D, Q, QN, RN);

   input     CN, D, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, d_clock_i, IQN, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, RN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFRLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFFRLLX4 (CN, D, Q, QN, RN);

   input     CN, D, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_RN, d_clock_i, IQN, c_SH_D;

   not       i1  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, RN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFSQLLX0 (CN, D, Q, SN);

   input     CN, D, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SN, d_clock_i, c_SH_D;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i0  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, 1'b1, SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFSQLLX1 (CN, D, Q, SN);

   input     CN, D, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SN, d_clock_i, c_SH_D;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i0  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, 1'b1, SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFSQLLX2 (CN, D, Q, SN);

   input     CN, D, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SN, d_clock_i, c_SH_D;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i0  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, 1'b1, SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFSQLLX4 (CN, D, Q, SN);

   input     CN, D, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SN, d_clock_i, c_SH_D;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i0  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, 1'b1, SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFSLLX0 (CN, D, Q, QN, SN);

   input     CN, D, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SN, d_clock_i, IQN, c_SH_D;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i0  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, 1'b1, SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFSLLX1 (CN, D, Q, QN, SN);

   input     CN, D, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SN, d_clock_i, IQN, c_SH_D;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i0  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, 1'b1, SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFSLLX2 (CN, D, Q, QN, SN);

   input     CN, D, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SN, d_clock_i, IQN, c_SH_D;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i0  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, 1'b1, SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFFSLLX4 (CN, D, Q, QN, SN);

   input     CN, D, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SN, d_clock_i, IQN, c_SH_D;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i2  (IQ, delay_D, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i0  (clock_i, CN);
   u1_fd5    i2  (IQ, D, clock_i, 1'b1, SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFFLLX0 (CN, D, Q, QN);

   input     CN, D;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, d_clock_i, IQN;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN;

   not       i0  (clock_i, CN);
   u1_fd5    i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFFLLX1 (CN, D, Q, QN);

   input     CN, D;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, d_clock_i, IQN;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN;

   not       i0  (clock_i, CN);
   u1_fd5    i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFFLLX2 (CN, D, Q, QN);

   input     CN, D;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, d_clock_i, IQN;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN;

   not       i0  (clock_i, CN);
   u1_fd5    i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFFLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFFLLX4 (CN, D, Q, QN);

   input     CN, D;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, d_clock_i, IQN;

   not       i0  (d_clock_i, delay_CN);
   u1_fd5    i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_D);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN;

   not       i0  (clock_i, CN);
   u1_fd5    i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge CN => (Q +: D)) = (0.02, 0.02);

      (negedge CN => (QN -: D)) = (0.02, 0.02);

      $setuphold (negedge CN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFRQLLX0 (C, D, Q);

   input     C, D;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D;

   u1_fd5    i0  (IQ, delay_D, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u1_fd5    i0  (IQ, D, C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFRQLLX1 (C, D, Q);

   input     C, D;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D;

   u1_fd5    i0  (IQ, delay_D, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u1_fd5    i0  (IQ, D, C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFRQLLX2 (C, D, Q);

   input     C, D;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D;

   u1_fd5    i0  (IQ, delay_D, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u1_fd5    i0  (IQ, D, C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFRQLLX4 (C, D, Q);

   input     C, D;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D;

   u1_fd5    i0  (IQ, delay_D, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u1_fd5    i0  (IQ, D, C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRQLLX0 (C, D, Q, RN);

   input     C, D, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i1  (IQ, D, C, RN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRQLLX1 (C, D, Q, RN);

   input     C, D, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i1  (IQ, D, C, RN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRQLLX2 (C, D, Q, RN);

   input     C, D, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i1  (IQ, D, C, RN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRQLLX4 (C, D, Q, RN);

   input     C, D, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i1  (IQ, D, C, RN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRSQLLX0 (C, D, Q, RN, SN);

   input     C, D, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, delay_SN, c_SH_D;

   u1_fd5    i2  (IQ, delay_D, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i2  (IQ, D, C, RN, SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRSQLLX1 (C, D, Q, RN, SN);

   input     C, D, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, delay_SN, c_SH_D;

   u1_fd5    i2  (IQ, delay_D, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i2  (IQ, D, C, RN, SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRSQLLX2 (C, D, Q, RN, SN);

   input     C, D, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, delay_SN, c_SH_D;

   u1_fd5    i2  (IQ, delay_D, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i2  (IQ, D, C, RN, SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRSQLLX4 (C, D, Q, RN, SN);

   input     C, D, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, delay_SN, c_SH_D;

   u1_fd5    i2  (IQ, delay_D, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i2  (IQ, D, C, RN, SN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRSLLX0 (C, D, Q, QN, RN, SN);

   input     C, D, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, delay_SN, IQN, qin_and_set,
                   c_SH_D;

   u1_fd5    i2  (IQ, delay_D, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   and       i4  (qin_and_set, IQN, delay_SN);
   buf       i5  (Q, IQ);
   buf       i6  (QN, qin_and_set);

   checkrs   i7  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, qin_and_set, c_SH_D;

   u1_fd5    i2  (IQ, D, C, RN, SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   and       i4  (qin_and_set, IQN, SN);
   buf       i5  (Q, IQ);
   buf       i6  (QN, qin_and_set);

   checkrs   i7  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRSLLX1 (C, D, Q, QN, RN, SN);

   input     C, D, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, delay_SN, IQN, qin_and_set,
                   c_SH_D;

   u1_fd5    i2  (IQ, delay_D, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   and       i4  (qin_and_set, IQN, delay_SN);
   buf       i5  (Q, IQ);
   buf       i6  (QN, qin_and_set);

   checkrs   i7  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, qin_and_set, c_SH_D;

   u1_fd5    i2  (IQ, D, C, RN, SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   and       i4  (qin_and_set, IQN, SN);
   buf       i5  (Q, IQ);
   buf       i6  (QN, qin_and_set);

   checkrs   i7  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRSLLX2 (C, D, Q, QN, RN, SN);

   input     C, D, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, delay_SN, IQN, qin_and_set,
                   c_SH_D;

   u1_fd5    i2  (IQ, delay_D, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   and       i4  (qin_and_set, IQN, delay_SN);
   buf       i5  (Q, IQ);
   buf       i6  (QN, qin_and_set);

   checkrs   i7  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, qin_and_set, c_SH_D;

   u1_fd5    i2  (IQ, D, C, RN, SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   and       i4  (qin_and_set, IQN, SN);
   buf       i5  (Q, IQ);
   buf       i6  (QN, qin_and_set);

   checkrs   i7  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset and Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRSLLX4 (C, D, Q, QN, RN, SN);

   input     C, D, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, delay_SN, IQN, qin_and_set,
                   c_SH_D;

   u1_fd5    i2  (IQ, delay_D, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   and       i4  (qin_and_set, IQN, delay_SN);
   buf       i5  (Q, IQ);
   buf       i6  (QN, qin_and_set);

   checkrs   i7  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, qin_and_set, c_SH_D;

   u1_fd5    i2  (IQ, D, C, RN, SN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   and       i4  (qin_and_set, IQN, SN);
   buf       i5  (Q, IQ);
   buf       i6  (QN, qin_and_set);

   checkrs   i7  (c_SH_D, RN, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRLLX0 (C, D, Q, QN, RN);

   input     C, D, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, IQN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u1_fd5    i1  (IQ, D, C, RN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRLLX1 (C, D, Q, QN, RN);

   input     C, D, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, IQN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u1_fd5    i1  (IQ, D, C, RN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRLLX2 (C, D, Q, QN, RN);

   input     C, D, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, IQN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u1_fd5    i1  (IQ, D, C, RN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRRLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset
//   last modified by : XLICDD generated
//****************************************************************************

module DFRRLLX4 (C, D, Q, QN, RN);

   input     C, D, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_RN, IQN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u1_fd5    i1  (IQ, D, C, RN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge RN => (Q +: RN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRSQLLX0 (C, D, Q, SN);

   input     C, D, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i1  (IQ, D, C, 1'b1, SN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRSQLLX1 (C, D, Q, SN);

   input     C, D, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i1  (IQ, D, C, 1'b1, SN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRSQLLX2 (C, D, Q, SN);

   input     C, D, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i1  (IQ, D, C, 1'b1, SN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRSQLLX4 (C, D, Q, SN);

   input     C, D, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u1_fd5    i1  (IQ, D, C, 1'b1, SN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRSLLX0 (C, D, Q, QN, SN);

   input     C, D, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SN, IQN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u1_fd5    i1  (IQ, D, C, 1'b1, SN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRSLLX1 (C, D, Q, QN, SN);

   input     C, D, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SN, IQN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u1_fd5    i1  (IQ, D, C, 1'b1, SN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRSLLX2 (C, D, Q, QN, SN);

   input     C, D, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SN, IQN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u1_fd5    i1  (IQ, D, C, 1'b1, SN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Set
//   last modified by : XLICDD generated
//****************************************************************************

module DFRSLLX4 (C, D, Q, QN, SN);

   input     C, D, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SN, IQN, c_SH_D;

   u1_fd5    i1  (IQ, delay_D, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u1_fd5    i1  (IQ, D, C, 1'b1, SN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q +: D)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFRLLX0 (C, D, Q, QN);

   input     C, D;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, IQN;

   u1_fd5    i0  (IQ, delay_D, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN;

   u1_fd5    i0  (IQ, D, C, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFRLLX1 (C, D, Q, QN);

   input     C, D;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, IQN;

   u1_fd5    i0  (IQ, delay_D, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN;

   u1_fd5    i0  (IQ, D, C, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFRLLX2 (C, D, Q, QN);

   input     C, D;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, IQN;

   u1_fd5    i0  (IQ, delay_D, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN;

   u1_fd5    i0  (IQ, D, C, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DFRLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop
//   last modified by : XLICDD generated
//****************************************************************************

module DFRLLX4 (C, D, Q, QN);

   input     C, D;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, IQN;

   u1_fd5    i0  (IQ, delay_D, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_D);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN;

   u1_fd5    i0  (IQ, D, C, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge C => (Q +: D)) = (0.02, 0.02);

      (posedge C => (QN -: D)) = (0.02, 0.02);

      $setuphold (posedge C, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLHQLLX0 (D, G, Q);

   input     D, G;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D;

   u_ld6     i0  (IQ, delay_D, delay_G, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u_ld6     i0  (IQ, D, G, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLHQLLX1 (D, G, Q);

   input     D, G;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D;

   u_ld6     i0  (IQ, delay_D, delay_G, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u_ld6     i0  (IQ, D, G, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLHQLLX2 (D, G, Q);

   input     D, G;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D;

   u_ld6     i0  (IQ, delay_D, delay_G, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u_ld6     i0  (IQ, D, G, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLHQLLX4 (D, G, Q);

   input     D, G;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D;

   u_ld6     i0  (IQ, delay_D, delay_G, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u_ld6     i0  (IQ, D, G, 1'b1, 1'b1, NOTIFY_REG);
   buf       i1  (Q, IQ);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRQLLX0 (D, G, Q, RN);

   input     D, G, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, 1'b1, delay_RN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i1  (IQ, D, G, 1'b1, RN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRQLLX1 (D, G, Q, RN);

   input     D, G, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, 1'b1, delay_RN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i1  (IQ, D, G, 1'b1, RN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRQLLX2 (D, G, Q, RN);

   input     D, G, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, 1'b1, delay_RN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i1  (IQ, D, G, 1'b1, RN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRQLLX4 (D, G, Q, RN);

   input     D, G, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, 1'b1, delay_RN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i1  (IQ, D, G, 1'b1, RN, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with set
//                      and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRSQLLX0 (D, G, Q, RN, SN);

   input     D, G, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, delay_SN, c_SH_D;

   u_ld6     i2  (IQ, delay_D, delay_G, delay_SN, delay_RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i2  (IQ, D, G, SN, RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with set
//                      and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRSQLLX1 (D, G, Q, RN, SN);

   input     D, G, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, delay_SN, c_SH_D;

   u_ld6     i2  (IQ, delay_D, delay_G, delay_SN, delay_RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i2  (IQ, D, G, SN, RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with set
//                      and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRSQLLX2 (D, G, Q, RN, SN);

   input     D, G, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, delay_SN, c_SH_D;

   u_ld6     i2  (IQ, delay_D, delay_G, delay_SN, delay_RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i2  (IQ, D, G, SN, RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with set
//                      and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRSQLLX4 (D, G, Q, RN, SN);

   input     D, G, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, delay_SN, c_SH_D;

   u_ld6     i2  (IQ, delay_D, delay_G, delay_SN, delay_RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i2  (IQ, D, G, SN, RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with set and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRSLLX0 (D, G, Q, QN, RN, SN);

   input     D, G, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, delay_SN, IQN, c_SH_D;

   u_ld6     i2  (IQ, delay_D, delay_G, delay_SN, delay_RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i2  (IQ, D, G, SN, RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with set and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRSLLX1 (D, G, Q, QN, RN, SN);

   input     D, G, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, delay_SN, IQN, c_SH_D;

   u_ld6     i2  (IQ, delay_D, delay_G, delay_SN, delay_RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i2  (IQ, D, G, SN, RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with set and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRSLLX2 (D, G, Q, QN, RN, SN);

   input     D, G, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, delay_SN, IQN, c_SH_D;

   u_ld6     i2  (IQ, delay_D, delay_G, delay_SN, delay_RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i2  (IQ, D, G, SN, RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with set and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRSLLX4 (D, G, Q, QN, RN, SN);

   input     D, G, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, delay_SN, IQN, c_SH_D;

   u_ld6     i2  (IQ, delay_D, delay_G, delay_SN, delay_RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i2  (IQ, D, G, SN, RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRLLX0 (D, G, Q, QN, RN);

   input     D, G, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, IQN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, 1'b1, delay_RN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i1  (IQ, D, G, 1'b1, RN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRLLX1 (D, G, Q, QN, RN);

   input     D, G, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, IQN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, 1'b1, delay_RN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i1  (IQ, D, G, 1'b1, RN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRLLX2 (D, G, Q, QN, RN);

   input     D, G, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, IQN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, 1'b1, delay_RN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i1  (IQ, D, G, 1'b1, RN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHRLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLHRLLX4 (D, G, Q, QN, RN);

   input     D, G, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_RN, IQN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, 1'b1, delay_RN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i1  (IQ, D, G, 1'b1, RN, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLHSQLLX0 (D, G, Q, SN);

   input     D, G, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_SN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, delay_SN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i1  (IQ, D, G, SN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLHSQLLX1 (D, G, Q, SN);

   input     D, G, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_SN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, delay_SN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i1  (IQ, D, G, SN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLHSQLLX2 (D, G, Q, SN);

   input     D, G, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_SN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, delay_SN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i1  (IQ, D, G, SN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active single Q transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLHSQLLX4 (D, G, Q, SN);

   input     D, G, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_SN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, delay_SN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, c_SH_D;

   u_ld6     i1  (IQ, D, G, SN, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);

   checkrs   i3  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLHSLLX0 (D, G, Q, QN, SN);

   input     D, G, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_SN, IQN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, delay_SN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i1  (IQ, D, G, SN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLHSLLX1 (D, G, Q, QN, SN);

   input     D, G, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_SN, IQN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, delay_SN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i1  (IQ, D, G, SN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLHSLLX2 (D, G, Q, QN, SN);

   input     D, G, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_SN, IQN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, delay_SN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i1  (IQ, D, G, SN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLHSLLX4 (D, G, Q, QN, SN);

   input     D, G, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, delay_SN, IQN, c_SH_D;

   u_ld6     i1  (IQ, delay_D, delay_G, delay_SN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_G, delay_D);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_G);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN, c_SH_D;

   u_ld6     i1  (IQ, D, G, SN, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);

   checkrs   i5  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge G, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLHLLX0 (D, G, Q, QN);

   input     D, G;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, IQN;

   u_ld6     i0  (IQ, delay_D, delay_G, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN;

   u_ld6     i0  (IQ, D, G, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLHLLX1 (D, G, Q, QN);

   input     D, G;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, IQN;

   u_ld6     i0  (IQ, delay_D, delay_G, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN;

   u_ld6     i0  (IQ, D, G, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLHLLX2 (D, G, Q, QN);

   input     D, G;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, IQN;

   u_ld6     i0  (IQ, delay_D, delay_G, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN;

   u_ld6     i0  (IQ, D, G, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLHLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : high active transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLHLLX4 (D, G, Q, QN);

   input     D, G;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_G, delay_D, IQN;

   u_ld6     i0  (IQ, delay_D, delay_G, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_G,
                   delay_D);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, IQN;

   u_ld6     i0  (IQ, D, G, 1'b1, 1'b1, NOTIFY_REG);
   not       i1  (IQN, IQ);
   buf       i2  (Q, IQ);
   buf       i3  (QN, IQN);


// timing section:
   specify

      (posedge G => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (posedge G => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (negedge G, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge G, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (posedge G, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLLQLLX0 (D, GN, Q);

   input     D, GN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, d_clock_i;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, GN);
   u_ld6     i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLLQLLX1 (D, GN, Q);

   input     D, GN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, d_clock_i;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, GN);
   u_ld6     i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLLQLLX2 (D, GN, Q);

   input     D, GN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, d_clock_i;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, GN);
   u_ld6     i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLLQLLX4 (D, GN, Q);

   input     D, GN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, d_clock_i;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, GN);
   u_ld6     i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i2  (Q, IQ);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRQLLX0 (D, GN, Q, RN);

   input     D, GN, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, 1'b1, delay_RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, 1'b1, RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRQLLX1 (D, GN, Q, RN);

   input     D, GN, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, 1'b1, delay_RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, 1'b1, RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRQLLX2 (D, GN, Q, RN);

   input     D, GN, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, 1'b1, delay_RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, 1'b1, RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRQLLX4 (D, GN, Q, RN);

   input     D, GN, RN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, 1'b1, delay_RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, 1'b1, RN, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with set
//                      and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRSQLLX0 (D, GN, Q, RN, SN);

   input     D, GN, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, delay_SN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i3  (IQ, delay_D, d_clock_i, delay_SN, delay_RN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i3  (IQ, D, clock_i, SN, RN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with set
//                      and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRSQLLX1 (D, GN, Q, RN, SN);

   input     D, GN, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, delay_SN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i3  (IQ, delay_D, d_clock_i, delay_SN, delay_RN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i3  (IQ, D, clock_i, SN, RN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with set
//                      and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRSQLLX2 (D, GN, Q, RN, SN);

   input     D, GN, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, delay_SN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i3  (IQ, delay_D, d_clock_i, delay_SN, delay_RN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i3  (IQ, D, clock_i, SN, RN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with set
//                      and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRSQLLX4 (D, GN, Q, RN, SN);

   input     D, GN, RN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, delay_SN, d_clock_i, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i3  (IQ, delay_D, d_clock_i, delay_SN, delay_RN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i3  (IQ, D, clock_i, SN, RN, NOTIFY_REG);
   buf       i4  (Q, IQ);

   checkrs   i5  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with set and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRSLLX0 (D, GN, Q, QN, RN, SN);

   input     D, GN, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, delay_SN, d_clock_i, IQN,
                   c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i3  (IQ, delay_D, d_clock_i, delay_SN, delay_RN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   buf       i5  (Q, IQ);
   buf       i6  (QN, IQN);

   checkrs   i7  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i3  (IQ, D, clock_i, SN, RN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   buf       i5  (Q, IQ);
   buf       i6  (QN, IQN);

   checkrs   i7  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with set and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRSLLX1 (D, GN, Q, QN, RN, SN);

   input     D, GN, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, delay_SN, d_clock_i, IQN,
                   c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i3  (IQ, delay_D, d_clock_i, delay_SN, delay_RN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   buf       i5  (Q, IQ);
   buf       i6  (QN, IQN);

   checkrs   i7  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i3  (IQ, D, clock_i, SN, RN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   buf       i5  (Q, IQ);
   buf       i6  (QN, IQN);

   checkrs   i7  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with set and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRSLLX2 (D, GN, Q, QN, RN, SN);

   input     D, GN, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, delay_SN, d_clock_i, IQN,
                   c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i3  (IQ, delay_D, d_clock_i, delay_SN, delay_RN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   buf       i5  (Q, IQ);
   buf       i6  (QN, IQN);

   checkrs   i7  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i3  (IQ, D, clock_i, SN, RN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   buf       i5  (Q, IQ);
   buf       i6  (QN, IQN);

   checkrs   i7  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with set and reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRSLLX4 (D, GN, Q, QN, RN, SN);

   input     D, GN, RN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, delay_SN, d_clock_i, IQN,
                   c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i3  (IQ, delay_D, d_clock_i, delay_SN, delay_RN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   buf       i5  (Q, IQ);
   buf       i6  (QN, IQN);

   checkrs   i7  (c_SH_D, delay_RN, delay_SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i3  (IQ, D, clock_i, SN, RN, NOTIFY_REG);
   not       i4  (IQN, IQ);
   buf       i5  (Q, IQ);
   buf       i6  (QN, IQN);

   checkrs   i7  (c_SH_D, RN, SN);

// timing section:
   specify

      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRLLX0 (D, GN, Q, QN, RN);

   input     D, GN, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, d_clock_i, IQN, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, 1'b1, delay_RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, 1'b1, RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRLLX1 (D, GN, Q, QN, RN);

   input     D, GN, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, d_clock_i, IQN, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, 1'b1, delay_RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, 1'b1, RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRLLX2 (D, GN, Q, QN, RN);

   input     D, GN, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, d_clock_i, IQN, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, 1'b1, delay_RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, 1'b1, RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLRLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with reset
//   last modified by : XLICDD generated
//****************************************************************************

module DLLRLLX4 (D, GN, Q, QN, RN);

   input     D, GN, RN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_RN, d_clock_i, IQN, c_SH_D;

   not       i1  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, 1'b1, delay_RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, delay_RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i1  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, 1'b1, RN, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, RN, 1'b1);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);
      (RN -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLLSQLLX0 (D, GN, Q, SN);

   input     D, GN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_SN, d_clock_i, c_SH_D;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, delay_SN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i0  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, SN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLLSQLLX1 (D, GN, Q, SN);

   input     D, GN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_SN, d_clock_i, c_SH_D;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, delay_SN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i0  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, SN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLLSQLLX2 (D, GN, Q, SN);

   input     D, GN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_SN, d_clock_i, c_SH_D;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, delay_SN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i0  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, SN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active single Q transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLLSQLLX4 (D, GN, Q, SN);

   input     D, GN, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_SN, d_clock_i, c_SH_D;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, delay_SN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, c_SH_D;

   not       i0  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, SN, 1'b1, NOTIFY_REG);
   buf       i3  (Q, IQ);

   checkrs   i4  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLLSLLX0 (D, GN, Q, QN, SN);

   input     D, GN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_SN, d_clock_i, IQN, c_SH_D;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, delay_SN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i0  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, SN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLLSLLX1 (D, GN, Q, QN, SN);

   input     D, GN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_SN, d_clock_i, IQN, c_SH_D;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, delay_SN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i0  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, SN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLLSLLX2 (D, GN, Q, QN, SN);

   input     D, GN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_SN, d_clock_i, IQN, c_SH_D;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, delay_SN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i0  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, SN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch with set
//   last modified by : XLICDD generated
//****************************************************************************

module DLLSLLX4 (D, GN, Q, QN, SN);

   input     D, GN, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, delay_SN, d_clock_i, IQN, c_SH_D;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i2  (IQ, delay_D, d_clock_i, delay_SN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, delay_SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_GN, delay_D);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_GN);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN, c_SH_D;

   not       i0  (clock_i, GN);
   u_ld6     i2  (IQ, D, clock_i, SN, 1'b1, NOTIFY_REG);
   not       i3  (IQN, IQ);
   buf       i4  (Q, IQ);
   buf       i5  (QN, IQN);

   checkrs   i6  (c_SH_D, 1'b1, SN);

// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (SN -=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (SN +=> QN) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge GN, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLLLLX0 (D, GN, Q, QN);

   input     D, GN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, d_clock_i, IQN;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN;

   not       i0  (clock_i, GN);
   u_ld6     i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLLLLX1 (D, GN, Q, QN);

   input     D, GN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, d_clock_i, IQN;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN;

   not       i0  (clock_i, GN);
   u_ld6     i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLLLLX2 (D, GN, Q, QN);

   input     D, GN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, d_clock_i, IQN;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN;

   not       i0  (clock_i, GN);
   u_ld6     i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLLLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : low active transparent D-latch
//   last modified by : XLICDD generated
//****************************************************************************

module DLLLLX4 (D, GN, Q, QN);

   input     D, GN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_GN, delay_D, d_clock_i, IQN;

   not       i0  (d_clock_i, delay_GN);
   u_ld6     i1  (IQ, delay_D, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG,,, delay_GN,
                   delay_D);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, IQN;

   not       i0  (clock_i, GN);
   u_ld6     i1  (IQ, D, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i2  (IQN, IQ);
   buf       i3  (Q, IQ);
   buf       i4  (QN, IQN);


// timing section:
   specify

      (negedge GN => (Q -: Q)) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

      (negedge GN => (QN -: QN)) = (0.02, 0.02);
      (D -=> QN) = (0.02, 0.02);

      $setuphold (posedge GN, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge GN, negedge D, 0.02, 0.02, NOTIFY_REG);

      $width (negedge GN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLY1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Delay Cell 1ns
//                    : Note: used for the different technology modules 
//                            and for the different operating conditions
//                            the cell name does not express a related 
//                            cell delay as nanosecond-value.
//                            The exact cell delay has to be timing 
//                            analyzed at the choosen operating 
//                            conditions and with the backannotated
//                            routing parasitics.
//
//   last modified by : XLICDD generated
//*****************************************************************

module DLY1LLX0 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);



// timing section:
   specify

      (A +=> Q) = (0.84, 0.64);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLY1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Delay Cell 1ns
//                    : Note: used for the different technology modules 
//                            and for the different operating conditions
//                            the cell name does not express a related 
//                            cell delay as nanosecond-value.
//                            The exact cell delay has to be timing 
//                            analyzed at the choosen operating 
//                            conditions and with the backannotated
//                            routing parasitics.
//
//   last modified by : XLICDD generated
//*****************************************************************

module DLY1LLX1 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);



// timing section:
   specify

      (A +=> Q) = (0.74, 0.64);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLY2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Delay Cell 2ns
//                    : Note: used for the different technology modules 
//                            and for the different operating conditions
//                            the cell name does not express a related 
//                            cell delay as nanosecond-value.
//                            The exact cell delay has to be timing 
//                            analyzed at the choosen operating 
//                            conditions and with the backannotated
//                            routing parasitics.
//
//   last modified by : XLICDD generated
//*****************************************************************

module DLY2LLX0 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);



// timing section:
   specify

      (A +=> Q) = (1.80, 1.91);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLY2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Delay Cell 2ns
//                    : Note: used for the different technology modules 
//                            and for the different operating conditions
//                            the cell name does not express a related 
//                            cell delay as nanosecond-value.
//                            The exact cell delay has to be timing 
//                            analyzed at the choosen operating 
//                            conditions and with the backannotated
//                            routing parasitics.
//
//   last modified by : XLICDD generated
//*****************************************************************

module DLY2LLX1 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);



// timing section:
   specify

      (A +=> Q) = (1.84, 2.29);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLY4LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Delay Cell 4ns
//                    : Note: used for the different technology modules 
//                            and for the different operating conditions
//                            the cell name does not express a related 
//                            cell delay as nanosecond-value.
//                            The exact cell delay has to be timing 
//                            analyzed at the choosen operating 
//                            conditions and with the backannotated
//                            routing parasitics.
//
//   last modified by : XLICDD generated
//*****************************************************************

module DLY4LLX0 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);



// timing section:
   specify

      (A +=> Q) = (3.51, 4.08);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLY4LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Delay Cell 4ns
//                    : Note: used for the different technology modules 
//                            and for the different operating conditions
//                            the cell name does not express a related 
//                            cell delay as nanosecond-value.
//                            The exact cell delay has to be timing 
//                            analyzed at the choosen operating 
//                            conditions and with the backannotated
//                            routing parasitics.
//
//   last modified by : XLICDD generated
//*****************************************************************

module DLY4LLX1 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);



// timing section:
   specify

      (A +=> Q) = (3.60, 4.84);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLY8LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Delay Cell 8ns
//                    : Note: used for the different technology modules 
//                            and for the different operating conditions
//                            the cell name does not express a related 
//                            cell delay as nanosecond-value.
//                            The exact cell delay has to be timing 
//                            analyzed at the choosen operating 
//                            conditions and with the backannotated
//                            routing parasitics.
//
//   last modified by : XLICDD generated
//*****************************************************************

module DLY8LLX0 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);



// timing section:
   specify

      (A +=> Q) = (7.46, 8.32);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DLY8LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Delay Cell 8ns
//                    : Note: used for the different technology modules 
//                            and for the different operating conditions
//                            the cell name does not express a related 
//                            cell delay as nanosecond-value.
//                            The exact cell delay has to be timing 
//                            analyzed at the choosen operating 
//                            conditions and with the backannotated
//                            routing parasitics.
//
//   last modified by : XLICDD generated
//*****************************************************************

module DLY8LLX1 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);



// timing section:
   specify

      (A +=> Q) = (8.55, 10.05);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XNOR
//   last modified by : XLICDD generated
//****************************************************************************

module EN2LLX0 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A^B)
   xnor      i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XNOR
//   last modified by : XLICDD generated
//****************************************************************************

module EN2LLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A^B)
   xnor      i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XNOR
//   last modified by : XLICDD generated
//****************************************************************************

module EN2LLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A^B)
   xnor      i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XNOR
//   last modified by : XLICDD generated
//****************************************************************************

module EN2LLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A^B)
   xnor      i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN2SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XNOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module EN2SLLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A^B)
   xnor      i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN2SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XNOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module EN2SLLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A^B)
   xnor      i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN2SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XNOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module EN2SLLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A^B)
   xnor      i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN2SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XNOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module EN2SLLX6 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A^B)
   xnor      i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input XNOR
//   last modified by : XLICDD generated
//****************************************************************************

module EN3LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A^B^C)
   xnor      i0  (Q, A, B, C);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1) || (B == 1'b1 && C == 1'b0)) (A +=> Q) =
                   (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0)) (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (posedge C => (Q +: Q)) = (0.02, 0.02);
      (negedge C => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input XNOR
//   last modified by : XLICDD generated
//****************************************************************************

module EN3LLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A^B^C)
   xnor      i0  (Q, A, B, C);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1) || (B == 1'b1 && C == 1'b0)) (A +=> Q) =
                   (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0)) (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (posedge C => (Q +: Q)) = (0.02, 0.02);
      (negedge C => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input XNOR
//   last modified by : XLICDD generated
//****************************************************************************

module EN3LLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A^B^C)
   xnor      i0  (Q, A, B, C);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1) || (B == 1'b1 && C == 1'b0)) (A +=> Q) =
                   (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0)) (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (posedge C => (Q +: Q)) = (0.02, 0.02);
      (negedge C => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EN3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input XNOR
//   last modified by : XLICDD generated
//****************************************************************************

module EN3LLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A^B^C)
   xnor      i0  (Q, A, B, C);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1) || (B == 1'b1 && C == 1'b0)) (A +=> Q) =
                   (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0)) (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (posedge C => (Q +: Q)) = (0.02, 0.02);
      (negedge C => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XOR
//   last modified by : XLICDD generated
//****************************************************************************

module EO2LLX0 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A^B)
   xor       i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XOR
//   last modified by : XLICDD generated
//****************************************************************************

module EO2LLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A^B)
   xor       i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XOR
//   last modified by : XLICDD generated
//****************************************************************************

module EO2LLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A^B)
   xor       i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XOR
//   last modified by : XLICDD generated
//****************************************************************************

module EO2LLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A^B)
   xor       i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO2SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module EO2SLLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A^B)
   xor       i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO2SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module EO2SLLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A^B)
   xor       i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO2SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module EO2SLLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A^B)
   xor       i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO2SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input XOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module EO2SLLX6 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A^B)
   xor       i0  (Q, A, B);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b1)) (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input XOR
//   last modified by : XLICDD generated
//****************************************************************************

module EO3LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A^B^C)
   xor       i0  (Q, A, B, C);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1) || (B == 1'b1 && C == 1'b0)) (A -=> Q) =
                   (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0)) (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: Q)) = (0.02, 0.02);
      (negedge C => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input XOR
//   last modified by : XLICDD generated
//****************************************************************************

module EO3LLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A^B^C)
   xor       i0  (Q, A, B, C);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1) || (B == 1'b1 && C == 1'b0)) (A -=> Q) =
                   (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0)) (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: Q)) = (0.02, 0.02);
      (negedge C => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input XOR
//   last modified by : XLICDD generated
//****************************************************************************

module EO3LLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A^B^C)
   xor       i0  (Q, A, B, C);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1) || (B == 1'b1 && C == 1'b0)) (A -=> Q) =
                   (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0)) (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: Q)) = (0.02, 0.02);
      (negedge C => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : EO3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input XOR
//   last modified by : XLICDD generated
//****************************************************************************

module EO3LLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A^B^C)
   xor       i0  (Q, A, B, C);

// timing section:
   specify

      (posedge A => (Q +: Q)) = (0.02, 0.02);
      (negedge A => (Q +: Q)) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1) || (B == 1'b1 && C == 1'b0)) (A -=> Q) =
                   (0.02, 0.02);
      (posedge B => (Q +: Q)) = (0.02, 0.02);
      (negedge B => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1)) (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b0)) (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && C == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (posedge C => (Q +: Q)) = (0.02, 0.02);
      (negedge C => (Q +: Q)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0)) (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FALLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Full Adder
//   last modified by : XLICDD generated
//****************************************************************************

module FALLX0 (A, B, CI, CO, S);

   input     A, B, CI;
   output    CO, S;
   wire n_0, n_1, n_2;

// Function CO: (B*CI) + (A*CI) + (A*B)
   and       i0  (n_0, B, CI);
   and       i1  (n_1, A, CI);
   and       i2  (n_2, A, B);
   or        i3  (CO, n_0, n_1, n_2);

// Function S: (A^B^CI)
   xor       i4  (S, A, B, CI);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);
      (CI +=> CO) = (0.02, 0.02);

      (posedge A => (S +: S)) = (0.02, 0.02);
      (negedge A => (S +: S)) = (0.02, 0.02);
         if (((B == 1'b0 && CI == 1'b0))) (A +=> S) = (0.02, 0.02);
         if ((B == 1'b1 && CI == 1'b0)) (A -=> S) = (0.02, 0.02);
         if (((B == 1'b1 && CI == 1'b1))) (A +=> S) = (0.02, 0.02);
      (posedge B => (S +: S)) = (0.02, 0.02);
      (negedge B => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0 && CI == 1'b0)) (B +=> S) = (0.02, 0.02);
         if ((A == 1'b1 && CI == 1'b0)) (B -=> S) = (0.02, 0.02);
         if ((A == 1'b1 && CI == 1'b1)) (B +=> S) = (0.02, 0.02);
      (posedge CI => (S +: S)) = (0.02, 0.02);
      (negedge CI => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0)) (CI +=> S) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (CI -=> S) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (CI +=> S) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FALLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Full Adder
//   last modified by : XLICDD generated
//****************************************************************************

module FALLX1 (A, B, CI, CO, S);

   input     A, B, CI;
   output    CO, S;
   wire n_0, n_1, n_2;

// Function CO: (B*CI) + (A*CI) + (A*B)
   and       i0  (n_0, B, CI);
   and       i1  (n_1, A, CI);
   and       i2  (n_2, A, B);
   or        i3  (CO, n_0, n_1, n_2);

// Function S: (A^B^CI)
   xor       i4  (S, A, B, CI);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);
      (CI +=> CO) = (0.02, 0.02);

      (posedge A => (S +: S)) = (0.02, 0.02);
      (negedge A => (S +: S)) = (0.02, 0.02);
         if (((B == 1'b0 && CI == 1'b0))) (A +=> S) = (0.02, 0.02);
         if ((B == 1'b1 && CI == 1'b0)) (A -=> S) = (0.02, 0.02);
         if (((B == 1'b1 && CI == 1'b1))) (A +=> S) = (0.02, 0.02);
      (posedge B => (S +: S)) = (0.02, 0.02);
      (negedge B => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0 && CI == 1'b0)) (B +=> S) = (0.02, 0.02);
         if ((A == 1'b1 && CI == 1'b0)) (B -=> S) = (0.02, 0.02);
         if ((A == 1'b1 && CI == 1'b1)) (B +=> S) = (0.02, 0.02);
      (posedge CI => (S +: S)) = (0.02, 0.02);
      (negedge CI => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0)) (CI +=> S) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (CI -=> S) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (CI +=> S) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FALLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Full Adder
//   last modified by : XLICDD generated
//****************************************************************************

module FALLX2 (A, B, CI, CO, S);

   input     A, B, CI;
   output    CO, S;
   wire n_0, n_1, n_2;

// Function CO: (B*CI) + (A*CI) + (A*B)
   and       i0  (n_0, B, CI);
   and       i1  (n_1, A, CI);
   and       i2  (n_2, A, B);
   or        i3  (CO, n_0, n_1, n_2);

// Function S: (A^B^CI)
   xor       i4  (S, A, B, CI);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);
      (CI +=> CO) = (0.02, 0.02);

      (posedge A => (S +: S)) = (0.02, 0.02);
      (negedge A => (S +: S)) = (0.02, 0.02);
         if (((B == 1'b0 && CI == 1'b0))) (A +=> S) = (0.02, 0.02);
         if ((B == 1'b1 && CI == 1'b0)) (A -=> S) = (0.02, 0.02);
         if (((B == 1'b1 && CI == 1'b1))) (A +=> S) = (0.02, 0.02);
      (posedge B => (S +: S)) = (0.02, 0.02);
      (negedge B => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0 && CI == 1'b0)) (B +=> S) = (0.02, 0.02);
         if ((A == 1'b1 && CI == 1'b0)) (B -=> S) = (0.02, 0.02);
         if ((A == 1'b1 && CI == 1'b1)) (B +=> S) = (0.02, 0.02);
      (posedge CI => (S +: S)) = (0.02, 0.02);
      (negedge CI => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0)) (CI +=> S) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (CI -=> S) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (CI +=> S) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FALLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Full Adder
//   last modified by : XLICDD generated
//****************************************************************************

module FALLX4 (A, B, CI, CO, S);

   input     A, B, CI;
   output    CO, S;
   wire n_0, n_1, n_2;

// Function CO: (B*CI) + (A*CI) + (A*B)
   and       i0  (n_0, B, CI);
   and       i1  (n_1, A, CI);
   and       i2  (n_2, A, B);
   or        i3  (CO, n_0, n_1, n_2);

// Function S: (A^B^CI)
   xor       i4  (S, A, B, CI);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);
      (CI +=> CO) = (0.02, 0.02);

      (posedge A => (S +: S)) = (0.02, 0.02);
      (negedge A => (S +: S)) = (0.02, 0.02);
         if (((B == 1'b0 && CI == 1'b0))) (A +=> S) = (0.02, 0.02);
         if ((B == 1'b1 && CI == 1'b0)) (A -=> S) = (0.02, 0.02);
         if (((B == 1'b1 && CI == 1'b1))) (A +=> S) = (0.02, 0.02);
      (posedge B => (S +: S)) = (0.02, 0.02);
      (negedge B => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0 && CI == 1'b0)) (B +=> S) = (0.02, 0.02);
         if ((A == 1'b1 && CI == 1'b0)) (B -=> S) = (0.02, 0.02);
         if ((A == 1'b1 && CI == 1'b1)) (B +=> S) = (0.02, 0.02);
      (posedge CI => (S +: S)) = (0.02, 0.02);
      (negedge CI => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0)) (CI +=> S) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (CI -=> S) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b1)) (CI +=> S) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : HALLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Half Adder
//   last modified by : XLICDD generated
//****************************************************************************

module HALLX0 (A, B, CO, S);

   input     A, B;
   output    CO, S;

// Function CO: (A*B)
   and       i0  (CO, A, B);

// Function S: (A^B)
   xor       i1  (S, A, B);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);

      (posedge A => (S +: S)) = (0.02, 0.02);
      (negedge A => (S +: S)) = (0.02, 0.02);
         if ((B == 1'b0)) (A +=> S) = (0.02, 0.02);
      (posedge B => (S +: S)) = (0.02, 0.02);
      (negedge B => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0)) (B +=> S) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : HALLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Half Adder
//   last modified by : XLICDD generated
//****************************************************************************

module HALLX1 (A, B, CO, S);

   input     A, B;
   output    CO, S;

// Function CO: (A*B)
   and       i0  (CO, A, B);

// Function S: (A^B)
   xor       i1  (S, A, B);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);

      (posedge A => (S +: S)) = (0.02, 0.02);
      (negedge A => (S +: S)) = (0.02, 0.02);
         if ((B == 1'b0)) (A +=> S) = (0.02, 0.02);
      (posedge B => (S +: S)) = (0.02, 0.02);
      (negedge B => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0)) (B +=> S) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : HALLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Half Adder
//   last modified by : XLICDD generated
//****************************************************************************

module HALLX2 (A, B, CO, S);

   input     A, B;
   output    CO, S;

// Function CO: (A*B)
   and       i0  (CO, A, B);

// Function S: (A^B)
   xor       i1  (S, A, B);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);

      (posedge A => (S +: S)) = (0.02, 0.02);
      (negedge A => (S +: S)) = (0.02, 0.02);
         if ((B == 1'b0)) (A +=> S) = (0.02, 0.02);
      (posedge B => (S +: S)) = (0.02, 0.02);
      (negedge B => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0)) (B +=> S) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : HALLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Half Adder
//   last modified by : XLICDD generated
//****************************************************************************

module HALLX4 (A, B, CO, S);

   input     A, B;
   output    CO, S;

// Function CO: (A*B)
   and       i0  (CO, A, B);

// Function S: (A^B)
   xor       i1  (S, A, B);

// timing section:
   specify

      (A +=> CO) = (0.02, 0.02);
      (B +=> CO) = (0.02, 0.02);

      (posedge A => (S +: S)) = (0.02, 0.02);
      (negedge A => (S +: S)) = (0.02, 0.02);
         if ((B == 1'b0)) (A +=> S) = (0.02, 0.02);
      (posedge B => (S +: S)) = (0.02, 0.02);
      (negedge B => (S +: S)) = (0.02, 0.02);
         if ((A == 1'b0)) (B +=> S) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : INLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Inverter
//   last modified by : XLICDD generated
//****************************************************************************

module INLLX0 (A, Q);

   input     A;
   output    Q;

// Function Q: !(A)
   not       i0  (Q, A);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : INLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Inverter
//   last modified by : XLICDD generated
//****************************************************************************

module INLLX1 (A, Q);

   input     A;
   output    Q;

// Function Q: !(A)
   not       i0  (Q, A);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : INLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Inverter
//   last modified by : XLICDD generated
//****************************************************************************

module INLLX2 (A, Q);

   input     A;
   output    Q;

// Function Q: !(A)
   not       i0  (Q, A);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : INLLX3
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Inverter
//   last modified by : XLICDD generated
//****************************************************************************

module INLLX3 (A, Q);

   input     A;
   output    Q;

// Function Q: !(A)
   not       i0  (Q, A);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : INLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Inverter
//   last modified by : XLICDD generated
//****************************************************************************

module INLLX4 (A, Q);

   input     A;
   output    Q;

// Function Q: !(A)
   not       i0  (Q, A);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : INLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Inverter
//   last modified by : XLICDD generated
//****************************************************************************

module INLLX6 (A, Q);

   input     A;
   output    Q;

// Function Q: !(A)
   not       i0  (Q, A);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : INLLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Inverter
//   last modified by : XLICDD generated
//****************************************************************************

module INLLX8 (A, Q);

   input     A;
   output    Q;

// Function Q: !(A)
   not       i0  (Q, A);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : INLLX12
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Inverter
//   last modified by : XLICDD generated
//****************************************************************************

module INLLX12 (A, Q);

   input     A;
   output    Q;

// Function Q: !(A)
   not       i0  (Q, A);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : INLLX16
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Inverter
//   last modified by : XLICDD generated
//****************************************************************************

module INLLX16 (A, Q);

   input     A;
   output    Q;

// Function Q: !(A)
   not       i0  (Q, A);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : INLLX20
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Inverter
//   last modified by : XLICDD generated
//****************************************************************************

module INLLX20 (A, Q);

   input     A;
   output    Q;

// Function Q: !(A)
   not       i0  (Q, A);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHCLLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ITHCLLX8 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHCLLX12
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ITHCLLX12 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHCLLX16
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ITHCLLX16 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHCLLX20
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ITHCLLX20 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITHLLX0 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITHLLX1 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITHLLX2 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHLLX3
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITHLLX3 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITHLLX4 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITHLLX6 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHLLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITHLLX8 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHLLX12
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITHLLX12 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHLLX16
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITHLLX16 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITHLLX20
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active high Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITHLLX20 (A, E, Q);

   input     A, E;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: !E
   not       i0  (n_0, A);
   bufif1    i2  (Q, n_0, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (E  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLCLLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ITLCLLX8 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLCLLX12
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ITLCLLX12 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLCLLX16
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ITLCLLX16 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLCLLX20
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable, cross
//                      current optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ITLCLLX20 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITLLLX0 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITLLLX1 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITLLLX2 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLLLX3
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITLLLX3 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITLLLX4 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITLLLX6 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLLLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITLLLX8 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLLLX12
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITLLLX12 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLLLX16
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITLLLX16 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ITLLLX20
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Tristate Inverter with active low Enable
//   last modified by : XLICDD generated
//****************************************************************************

module ITLLLX20 (A, EN, Q);

   input     A, EN;
   output    Q;
   wire n_0;

// Function Q: !(A); Tristate function: EN
   not       i0  (n_0, A);
   bufif0    i1  (Q, n_0, EN);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (EN  => Q) = (0.02, 0.02, 0.02, 0.02, 0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LGCNLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge clock gating cell, high latch Enable
//   last modified by : XLICDD generated
//****************************************************************************

module LGCNLLX0 (CLK, E, GCLK);

   input     CLK, E;
   output    GCLK;
   wire n_0;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E;

   u_ld1     i0  (IQ, delay_E, delay_CLK, NOTIFY_REG);
   not       i1  (n_0, IQ);
   or        i2  (GCLK, delay_CLK, n_0);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u_ld1     i0  (IQ, E, CLK, NOTIFY_REG);
   not       i1  (n_0, IQ);
   or        i2  (GCLK, CLK, n_0);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LGCNLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge clock gating cell, high latch Enable
//   last modified by : XLICDD generated
//****************************************************************************

module LGCNLLX1 (CLK, E, GCLK);

   input     CLK, E;
   output    GCLK;
   wire n_0;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E;

   u_ld1     i0  (IQ, delay_E, delay_CLK, NOTIFY_REG);
   not       i1  (n_0, IQ);
   or        i2  (GCLK, delay_CLK, n_0);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u_ld1     i0  (IQ, E, CLK, NOTIFY_REG);
   not       i1  (n_0, IQ);
   or        i2  (GCLK, CLK, n_0);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LGCNLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge clock gating cell, high latch Enable
//   last modified by : XLICDD generated
//****************************************************************************

module LGCNLLX2 (CLK, E, GCLK);

   input     CLK, E;
   output    GCLK;
   wire n_0;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E;

   u_ld1     i0  (IQ, delay_E, delay_CLK, NOTIFY_REG);
   not       i1  (n_0, IQ);
   or        i2  (GCLK, delay_CLK, n_0);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u_ld1     i0  (IQ, E, CLK, NOTIFY_REG);
   not       i1  (n_0, IQ);
   or        i2  (GCLK, CLK, n_0);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LGCPLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge clock gating cell, low latch Enable
//   last modified by : XLICDD generated
//****************************************************************************

module LGCPLLX0 (CLK, E, GCLK);

   input     CLK, E;
   output    GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, d_clock_i;

   not       i0  (d_clock_i, delay_CLK);
   u_ld1     i1  (IQ, delay_E, d_clock_i, NOTIFY_REG);
   and       i2  (GCLK, delay_CLK, IQ);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, CLK);
   u_ld1     i1  (IQ, E, clock_i, NOTIFY_REG);
   and       i2  (GCLK, CLK, IQ);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LGCPLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge clock gating cell, low latch Enable
//   last modified by : XLICDD generated
//****************************************************************************

module LGCPLLX1 (CLK, E, GCLK);

   input     CLK, E;
   output    GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, d_clock_i;

   not       i0  (d_clock_i, delay_CLK);
   u_ld1     i1  (IQ, delay_E, d_clock_i, NOTIFY_REG);
   and       i2  (GCLK, delay_CLK, IQ);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, CLK);
   u_ld1     i1  (IQ, E, clock_i, NOTIFY_REG);
   and       i2  (GCLK, CLK, IQ);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LGCPLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge clock gating cell, low latch Enable
//   last modified by : XLICDD generated
//****************************************************************************

module LGCPLLX2 (CLK, E, GCLK);

   input     CLK, E;
   output    GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, d_clock_i;

   not       i0  (d_clock_i, delay_CLK);
   u_ld1     i1  (IQ, delay_E, d_clock_i, NOTIFY_REG);
   and       i2  (GCLK, delay_CLK, IQ);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i;

   not       i0  (clock_i, CLK);
   u_ld1     i1  (IQ, E, clock_i, NOTIFY_REG);
   and       i2  (GCLK, CLK, IQ);


// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSGCNLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge clock gating cell, high latch Enable, scan
//                      precontrol
//   last modified by : XLICDD generated
//****************************************************************************

module LSGCNLLX0 (CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    GCLK;
   wire n_0;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, delay_SE, d_dat_i, c_SH_E, c_SH_SE,
                   c_SH_E_v, c_SH_SE_v;

   or        i0  (d_dat_i, delay_E, delay_SE);
   u_ld1     i1  (IQ, d_dat_i, delay_CLK, NOTIFY_REG);
   not       i2  (n_0, IQ);
   or        i3  (GCLK, delay_CLK, n_0);

   not       i4  (c_SH_E, delay_SE);
   not       i5  (c_SH_SE, delay_E);
   not #(0.01) i6  (c_SH_E_v, delay_SE);
   not #(0.01) i7  (c_SH_SE_v, delay_E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_E);
      $setuphold (negedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CLK, delay_E);

      $setuphold (negedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);
      $setuphold (negedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_E, c_SH_SE, c_SH_E_v, c_SH_SE_v;

   or        i0  (dat_i, E, SE);
   u_ld1     i1  (IQ, dat_i, CLK, NOTIFY_REG);
   not       i2  (n_0, IQ);
   or        i3  (GCLK, CLK, n_0);

   not       i4  (c_SH_E, SE);
   not       i5  (c_SH_SE, E);
   not #(0.01) i6  (c_SH_E_v, SE);
   not #(0.01) i7  (c_SH_SE_v, E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (negedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (negedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSGCNLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge clock gating cell, high latch Enable, scan
//                      precontrol
//   last modified by : XLICDD generated
//****************************************************************************

module LSGCNLLX1 (CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, delay_SE, d_dat_i, c_SH_E, c_SH_SE,
                   c_SH_E_v, c_SH_SE_v;

   or        i0  (d_dat_i, delay_E, delay_SE);
   u_ld1     i1  (IQ, d_dat_i, delay_CLK, NOTIFY_REG);
   not       i2  (n_0, IQ);
   or        i3  (GCLK, delay_CLK, n_0);

   not       i4  (c_SH_E, delay_SE);
   not       i5  (c_SH_SE, delay_E);
   not #(0.01) i6  (c_SH_E_v, delay_SE);
   not #(0.01) i7  (c_SH_SE_v, delay_E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_E);
      $setuphold (negedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CLK, delay_E);

      $setuphold (negedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);
      $setuphold (negedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_E, c_SH_SE, c_SH_E_v, c_SH_SE_v, n_0;

   or        i0  (dat_i, E, SE);
   u_ld1     i1  (IQ, dat_i, CLK, NOTIFY_REG);
   not       i2  (n_0, IQ);
   or        i3  (GCLK, CLK, n_0);

   not       i4  (c_SH_E, SE);
   not       i5  (c_SH_SE, E);
   not #(0.01) i6  (c_SH_E_v, SE);
   not #(0.01) i7  (c_SH_SE_v, E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (negedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (negedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSGCNLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge clock gating cell, high latch Enable, scan
//                      precontrol
//   last modified by : XLICDD generated
//****************************************************************************

module LSGCNLLX2 (CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, delay_SE, d_dat_i, c_SH_E, c_SH_SE,
                   c_SH_E_v, c_SH_SE_v;

   or        i0  (d_dat_i, delay_E, delay_SE);
   u_ld1     i1  (IQ, d_dat_i, delay_CLK, NOTIFY_REG);
   not       i2  (n_0, IQ);
   or        i3  (GCLK, delay_CLK, n_0);

   not       i4  (c_SH_E, delay_SE);
   not       i5  (c_SH_SE, delay_E);
   not #(0.01) i6  (c_SH_E_v, delay_SE);
   not #(0.01) i7  (c_SH_SE_v, delay_E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_E);
      $setuphold (negedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CLK, delay_E);

      $setuphold (negedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);
      $setuphold (negedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_E, c_SH_SE, c_SH_E_v, c_SH_SE_v, n_0;

   or        i0  (dat_i, E, SE);
   u_ld1     i1  (IQ, dat_i, CLK, NOTIFY_REG);
   not       i2  (n_0, IQ);
   or        i3  (GCLK, CLK, n_0);

   not       i4  (c_SH_E, SE);
   not       i5  (c_SH_SE, E);
   not #(0.01) i6  (c_SH_E_v, SE);
   not #(0.01) i7  (c_SH_SE_v, E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (negedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (negedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSGCPLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge clock gating cell, low latch Enable, scan
//                      precontrol
//   last modified by : XLICDD generated
//****************************************************************************

module LSGCPLLX0 (CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, delay_SE, d_clock_i, d_dat_i, c_SH_E,
                   c_SH_SE, c_SH_E_v, c_SH_SE_v;

   not       i0  (d_clock_i, delay_CLK);
   or        i1  (d_dat_i, delay_E, delay_SE);
   u_ld1     i2  (IQ, d_dat_i, d_clock_i, NOTIFY_REG);
   and       i3  (GCLK, delay_CLK, IQ);

   not       i4  (c_SH_E, delay_SE);
   not       i5  (c_SH_SE, delay_E);
   not #(0.01) i6  (c_SH_E_v, delay_SE);
   not #(0.01) i7  (c_SH_SE_v, delay_E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_E);
      $setuphold (posedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CLK, delay_E);

      $setuphold (posedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);
      $setuphold (posedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_E, c_SH_SE, c_SH_E_v, c_SH_SE_v;

   not       i0  (clock_i, CLK);
   or        i1  (dat_i, E, SE);
   u_ld1     i2  (IQ, dat_i, clock_i, NOTIFY_REG);
   and       i3  (GCLK, CLK, IQ);

   not       i4  (c_SH_E, SE);
   not       i5  (c_SH_SE, E);
   not #(0.01) i6  (c_SH_E_v, SE);
   not #(0.01) i7  (c_SH_SE_v, E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (posedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (posedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSGCPLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge clock gating cell, low latch Enable, scan
//                      precontrol
//   last modified by : XLICDD generated
//****************************************************************************

module LSGCPLLX1 (CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, delay_SE, d_clock_i, d_dat_i, c_SH_E,
                   c_SH_SE, c_SH_E_v, c_SH_SE_v;

   not       i0  (d_clock_i, delay_CLK);
   or        i1  (d_dat_i, delay_E, delay_SE);
   u_ld1     i2  (IQ, d_dat_i, d_clock_i, NOTIFY_REG);
   and       i3  (GCLK, delay_CLK, IQ);

   not       i4  (c_SH_E, delay_SE);
   not       i5  (c_SH_SE, delay_E);
   not #(0.01) i6  (c_SH_E_v, delay_SE);
   not #(0.01) i7  (c_SH_SE_v, delay_E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_E);
      $setuphold (posedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CLK, delay_E);

      $setuphold (posedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);
      $setuphold (posedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_E, c_SH_SE, c_SH_E_v, c_SH_SE_v;

   not       i0  (clock_i, CLK);
   or        i1  (dat_i, E, SE);
   u_ld1     i2  (IQ, dat_i, clock_i, NOTIFY_REG);
   and       i3  (GCLK, CLK, IQ);

   not       i4  (c_SH_E, SE);
   not       i5  (c_SH_SE, E);
   not #(0.01) i6  (c_SH_E_v, SE);
   not #(0.01) i7  (c_SH_SE_v, E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (posedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (posedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSGCPLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge clock gating cell, low latch Enable, scan
//                      precontrol
//   last modified by : XLICDD generated
//****************************************************************************

module LSGCPLLX2 (CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, delay_SE, d_clock_i, d_dat_i, c_SH_E,
                   c_SH_SE, c_SH_E_v, c_SH_SE_v;

   not       i0  (d_clock_i, delay_CLK);
   or        i1  (d_dat_i, delay_E, delay_SE);
   u_ld1     i2  (IQ, d_dat_i, d_clock_i, NOTIFY_REG);
   and       i3  (GCLK, delay_CLK, IQ);

   not       i4  (c_SH_E, delay_SE);
   not       i5  (c_SH_SE, delay_E);
   not #(0.01) i6  (c_SH_E_v, delay_SE);
   not #(0.01) i7  (c_SH_SE_v, delay_E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_E);
      $setuphold (posedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CLK, delay_E);

      $setuphold (posedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);
      $setuphold (posedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CLK, delay_SE);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_E, c_SH_SE, c_SH_E_v, c_SH_SE_v;

   not       i0  (clock_i, CLK);
   or        i1  (dat_i, E, SE);
   u_ld1     i2  (IQ, dat_i, clock_i, NOTIFY_REG);
   and       i3  (GCLK, CLK, IQ);

   not       i4  (c_SH_E, SE);
   not       i5  (c_SH_SE, E);
   not #(0.01) i6  (c_SH_E_v, SE);
   not #(0.01) i7  (c_SH_SE_v, E);

// timing section:
   specify

      (CLK +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E &&& c_SH_E_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (posedge CLK, negedge E &&& c_SH_E, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge CLK, posedge SE &&& c_SH_SE_v, 0.02, 0.02,
                   NOTIFY_REG);
      $setuphold (posedge CLK, negedge SE &&& c_SH_SE, 0.02, 0.02,
                   NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSOGCNLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge clock gating cell, high latch Enable, scan
//                      postcontrol, obs_output
//   last modified by : XLICDD generated
//****************************************************************************

module LSOGCNLLX0 (CGOBS, CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    CGOBS, GCLK;
   wire n_1;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E;

   u_ld1     i0  (IQ, delay_E, delay_CLK, NOTIFY_REG);
   buf       i1  (CGOBS, IQ);
   nor       i2  (n_1, IQ, SE);
   or        i4  (GCLK, delay_CLK, n_1);


// timing section:
   specify

      (E +=> CGOBS) = (0.02, 0.02);
      (posedge CLK => (CGOBS +: E)) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE -=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ;

   u_ld1     i0  (IQ, E, CLK, NOTIFY_REG);
   buf       i1  (CGOBS, IQ);
   nor       i2  (n_1, IQ, SE);
   or        i4  (GCLK, CLK, n_1);


// timing section:
   specify

      (E +=> CGOBS) = (0.02, 0.02);
      (posedge CLK => (CGOBS +: E)) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE -=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSOGCNLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge clock gating cell, high latch Enable, scan
//                      postcontrol, obs_output
//   last modified by : XLICDD generated
//****************************************************************************

module LSOGCNLLX1 (CGOBS, CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    CGOBS, GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E;

   u_ld1     i0  (IQ, delay_E, delay_CLK, NOTIFY_REG);
   buf       i1  (CGOBS, IQ);
   nor       i2  (n_1, IQ, SE);
   or        i4  (GCLK, delay_CLK, n_1);


// timing section:
   specify

      (E +=> CGOBS) = (0.02, 0.02);
      (posedge CLK => (CGOBS +: E)) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE -=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ,n_1;

   u_ld1     i0  (IQ, E, CLK, NOTIFY_REG);
   buf       i1  (CGOBS, IQ);
   nor       i2  (n_1, IQ, SE);
   or        i4  (GCLK, CLK, n_1);


// timing section:
   specify

      (E +=> CGOBS) = (0.02, 0.02);
      (posedge CLK => (CGOBS +: E)) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE -=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSOGCNLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge clock gating cell, high latch Enable, scan
//                      postcontrol, obs_output
//   last modified by : XLICDD generated
//****************************************************************************

module LSOGCNLLX2 (CGOBS, CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    CGOBS, GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E;

   u_ld1     i0  (IQ, delay_E, delay_CLK, NOTIFY_REG);
   buf       i1  (CGOBS, IQ);
   nor       i2  (n_1, IQ, SE);
   or        i4  (GCLK, delay_CLK, n_1);


// timing section:
   specify

      (E +=> CGOBS) = (0.02, 0.02);
      (posedge CLK => (CGOBS +: E)) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE -=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, n_1;

   u_ld1     i0  (IQ, E, CLK, NOTIFY_REG);
   buf       i1  (CGOBS, IQ);
   nor       i2  (n_1, IQ, SE);
   or        i4  (GCLK, CLK, n_1);


// timing section:
   specify

      (E +=> CGOBS) = (0.02, 0.02);
      (posedge CLK => (CGOBS +: E)) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE -=> GCLK) = (0.02, 0.02);

      $setuphold (negedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSOGCPLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge clock gating cell, low latch Enable, scan
//                      postcontrol, obs_output
//   last modified by : XLICDD generated
//****************************************************************************

module LSOGCPLLX0 (CGOBS, CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    CGOBS, GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, d_clock_i;

   not       i0  (d_clock_i, delay_CLK);
   u_ld1     i1  (IQ, delay_E, d_clock_i, NOTIFY_REG);
   buf       i2  (CGOBS, IQ);
   or        i3  (n_1, IQ, SE);
   and       i4  (GCLK, delay_CLK, n_1);


// timing section:
   specify

      (negedge CLK => (CGOBS +: E)) = (0.02, 0.02);
      (E +=> CGOBS) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, n_1;

   not       i0  (clock_i, CLK);
   u_ld1     i1  (IQ, E, clock_i, NOTIFY_REG);
   buf       i2  (CGOBS, IQ);
   or        i3  (n_1, IQ, SE);
   and       i4  (GCLK, CLK, n_1);


// timing section:
   specify

      (negedge CLK => (CGOBS +: E)) = (0.02, 0.02);
      (E +=> CGOBS) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSOGCPLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge clock gating cell, low latch Enable, scan
//                      postcontrol, obs_output
//   last modified by : XLICDD generated
//****************************************************************************

module LSOGCPLLX1 (CGOBS, CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    CGOBS, GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, d_clock_i;

   not       i0  (d_clock_i, delay_CLK);
   u_ld1     i1  (IQ, delay_E, d_clock_i, NOTIFY_REG);
   buf       i2  (CGOBS, IQ);
   or        i3  (n_1, IQ, SE);
   and       i4  (GCLK, delay_CLK, n_1);


// timing section:
   specify

      (negedge CLK => (CGOBS +: E)) = (0.02, 0.02);
      (E +=> CGOBS) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, n_1;

   not       i0  (clock_i, CLK);
   u_ld1     i1  (IQ, E, clock_i, NOTIFY_REG);
   buf       i2  (CGOBS, IQ);
   or        i3  (n_1, IQ, SE);
   and       i4  (GCLK, CLK, n_1);


// timing section:
   specify

      (negedge CLK => (CGOBS +: E)) = (0.02, 0.02);
      (E +=> CGOBS) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LSOGCPLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge clock gating cell, low latch Enable, scan
//                      postcontrol, obs_output
//   last modified by : XLICDD generated
//****************************************************************************

module LSOGCPLLX2 (CGOBS, CLK, E, GCLK, SE);

   input     CLK, E, SE;
   output    CGOBS, GCLK;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CLK, delay_E, d_clock_i;

   not       i0  (d_clock_i, delay_CLK);
   u_ld1     i1  (IQ, delay_E, d_clock_i, NOTIFY_REG);
   buf       i2  (CGOBS, IQ);
   or        i3  (n_1, IQ, SE);
   and       i4  (GCLK, delay_CLK, n_1);


// timing section:
   specify

      (negedge CLK => (CGOBS +: E)) = (0.02, 0.02);
      (E +=> CGOBS) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG,,, delay_CLK,
                   delay_E);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, n_1;

   not       i0  (clock_i, CLK);
   u_ld1     i1  (IQ, E, clock_i, NOTIFY_REG);
   buf       i2  (CGOBS, IQ);
   or        i3  (n_1, IQ, SE);
   and       i4  (GCLK, CLK, n_1);


// timing section:
   specify

      (negedge CLK => (CGOBS +: E)) = (0.02, 0.02);
      (E +=> CGOBS) = (0.02, 0.02);

      (CLK +=> GCLK) = (0.02, 0.02);
      (SE +=> GCLK) = (0.02, 0.02);

      $setuphold (posedge CLK, posedge E, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge CLK, negedge E, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CLK, 0.02, 0, NOTIFY_REG);
      $width (negedge CLK, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU2ILLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2:1 inverting Multiplexer
//   last modified by : XLICDD generated
//****************************************************************************

module MU2ILLX0 (IN0, IN1, Q, S);

   input     IN0, IN1, S;
   output    Q;
   wire n_3;

// Function Q: !((!S*IN0)+(S*IN1))
   u_mx2     i5  (n_3, IN0, IN1, S);
   not       i4  (Q, n_3);

// timing section:
   specify

      (posedge S => (Q +: Q)) = (0.02, 0.02);
      (negedge S => (Q +: Q)) = (0.02, 0.02);
         if ((IN0 == 1'b0 && IN1 == 1'b1)) (S -=> Q) = (0.02, 0.02);
      (IN0 -=> Q) = (0.02, 0.02);
      (IN1 -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU2ILLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2:1 inverting Multiplexer
//   last modified by : XLICDD generated
//****************************************************************************

module MU2ILLX1 (IN0, IN1, Q, S);

   input     IN0, IN1, S;
   output    Q;
   wire n_3;

// Function Q: !((!S*IN0)+(S*IN1))
   u_mx2     i5  (n_3, IN0, IN1, S);
   not       i4  (Q, n_3);

// timing section:
   specify

      (posedge S => (Q +: Q)) = (0.02, 0.02);
      (negedge S => (Q +: Q)) = (0.02, 0.02);
         if ((IN0 == 1'b0 && IN1 == 1'b1)) (S -=> Q) = (0.02, 0.02);
      (IN0 -=> Q) = (0.02, 0.02);
      (IN1 -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU2ILLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2:1 inverting Multiplexer
//   last modified by : XLICDD generated
//****************************************************************************

module MU2ILLX2 (IN0, IN1, Q, S);

   input     IN0, IN1, S;
   output    Q;
   wire n_3;

// Function Q: !((!S*IN0)+(S*IN1))
   u_mx2     i5  (n_3, IN0, IN1, S);
   not       i4  (Q, n_3);

// timing section:
   specify

      (posedge S => (Q +: Q)) = (0.02, 0.02);
      (negedge S => (Q +: Q)) = (0.02, 0.02);
         if ((IN0 == 1'b0 && IN1 == 1'b1)) (S -=> Q) = (0.02, 0.02);
      (IN0 -=> Q) = (0.02, 0.02);
      (IN1 -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU2ILLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2:1 inverting Multiplexer
//   last modified by : XLICDD generated
//****************************************************************************

module MU2ILLX4 (IN0, IN1, Q, S);

   input     IN0, IN1, S;
   output    Q;
   wire n_3;

// Function Q: !((!S*IN0)+(S*IN1))
   u_mx2     i5  (n_3, IN0, IN1, S);
   not       i4  (Q, n_3);

// timing section:
   specify

      (posedge S => (Q +: Q)) = (0.02, 0.02);
      (negedge S => (Q +: Q)) = (0.02, 0.02);
         if ((IN0 == 1'b0 && IN1 == 1'b1)) (S -=> Q) = (0.02, 0.02);
      (IN0 -=> Q) = (0.02, 0.02);
      (IN1 -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2:1 Multiplexer
//   last modified by : XLICDD generated
//****************************************************************************

module MU2LLX0 (IN0, IN1, Q, S);

   input     IN0, IN1, S;
   output    Q;

// Function Q: (!S*IN0)+(S*IN1)
   u_mx2     i4  (Q, IN0, IN1, S);

// timing section:
   specify

      (posedge S => (Q +: Q)) = (0.02, 0.02);
      (negedge S => (Q +: Q)) = (0.02, 0.02);
         if ((IN0 == 1'b0 && IN1 == 1'b1)) (S +=> Q) = (0.02, 0.02);
      (IN0 +=> Q) = (0.02, 0.02);
      (IN1 +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2:1 Multiplexer
//   last modified by : XLICDD generated
//****************************************************************************

module MU2LLX1 (IN0, IN1, Q, S);

   input     IN0, IN1, S;
   output    Q;

// Function Q: (!S*IN0)+(S*IN1)
   u_mx2     i4  (Q, IN0, IN1, S);

// timing section:
   specify

      (posedge S => (Q +: Q)) = (0.02, 0.02);
      (negedge S => (Q +: Q)) = (0.02, 0.02);
         if ((IN0 == 1'b0 && IN1 == 1'b1)) (S +=> Q) = (0.02, 0.02);
      (IN0 +=> Q) = (0.02, 0.02);
      (IN1 +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2:1 Multiplexer
//   last modified by : XLICDD generated
//****************************************************************************

module MU2LLX2 (IN0, IN1, Q, S);

   input     IN0, IN1, S;
   output    Q;

// Function Q: (!S*IN0)+(S*IN1)
   u_mx2     i4  (Q, IN0, IN1, S);

// timing section:
   specify

      (posedge S => (Q +: Q)) = (0.02, 0.02);
      (negedge S => (Q +: Q)) = (0.02, 0.02);
         if ((IN0 == 1'b0 && IN1 == 1'b1)) (S +=> Q) = (0.02, 0.02);
      (IN0 +=> Q) = (0.02, 0.02);
      (IN1 +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2:1 Multiplexer
//   last modified by : XLICDD generated
//****************************************************************************

module MU2LLX4 (IN0, IN1, Q, S);

   input     IN0, IN1, S;
   output    Q;

// Function Q: (!S*IN0)+(S*IN1)
   u_mx2     i4  (Q, IN0, IN1, S);

// timing section:
   specify

      (posedge S => (Q +: Q)) = (0.02, 0.02);
      (negedge S => (Q +: Q)) = (0.02, 0.02);
         if ((IN0 == 1'b0 && IN1 == 1'b1)) (S +=> Q) = (0.02, 0.02);
      (IN0 +=> Q) = (0.02, 0.02);
      (IN1 +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU4ILLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4:1 inverting Multiplexer
//   last modified by : XLICDD generated
//*****************************************************************

module MU4ILLX0 (IN0, IN1, IN2, IN3, Q, S0, S1);

   input   IN0, IN1, IN2, IN3, S0, S1;
   output  Q;
   wire n_0, n_1, n_2;

// logic section  
   u_mx2   i1 (n_0, IN0, IN1, S0);
   u_mx2   i2 (n_1, IN2, IN3, S0);
   u_mx2   i3 (n_2, n_0, n_1, S1);
   not     i4 (Q, n_2);
    

// timing section:
   specify

      (IN0 -=> Q) = (0.02, 0.02);
      (IN1 -=> Q) = (0.02, 0.02);
      (posedge S0 => (Q +: Q)) = (0.02, 0.02);
      (negedge S0 => (Q +: Q)) = (0.02, 0.02);
      (IN2 -=> Q) = (0.02, 0.02);
      (posedge S1 => (Q +: Q)) = (0.02, 0.02);
      (negedge S1 => (Q +: Q)) = (0.02, 0.02);
      (IN3 -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU4ILLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4:1 inverting Multiplexer
//   last modified by : XLICDD generated
//*****************************************************************

module MU4ILLX1 (IN0, IN1, IN2, IN3, Q, S0, S1);

   input   IN0, IN1, IN2, IN3, S0, S1;
   output  Q;
   wire n_0, n_1, n_2;

// logic section  
   u_mx2   i1 (n_0, IN0, IN1, S0);
   u_mx2   i2 (n_1, IN2, IN3, S0);
   u_mx2   i3 (n_2, n_0, n_1, S1);
   not     i4 (Q, n_2);
    

// timing section:
   specify

      (IN0 -=> Q) = (0.02, 0.02);
      (IN1 -=> Q) = (0.02, 0.02);
      (posedge S0 => (Q +: Q)) = (0.02, 0.02);
      (negedge S0 => (Q +: Q)) = (0.02, 0.02);
      (IN2 -=> Q) = (0.02, 0.02);
      (posedge S1 => (Q +: Q)) = (0.02, 0.02);
      (negedge S1 => (Q +: Q)) = (0.02, 0.02);
      (IN3 -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU4ILLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4:1 inverting Multiplexer
//   last modified by : XLICDD generated
//*****************************************************************

module MU4ILLX2 (IN0, IN1, IN2, IN3, Q, S0, S1);

   input   IN0, IN1, IN2, IN3, S0, S1;
   output  Q;
   wire n_0, n_1, n_2;

// logic section  
   u_mx2   i1 (n_0, IN0, IN1, S0);
   u_mx2   i2 (n_1, IN2, IN3, S0);
   u_mx2   i3 (n_2, n_0, n_1, S1);
   not     i4 (Q, n_2);
    

// timing section:
   specify

      (IN0 -=> Q) = (0.02, 0.02);
      (IN1 -=> Q) = (0.02, 0.02);
      (posedge S0 => (Q +: Q)) = (0.02, 0.02);
      (negedge S0 => (Q +: Q)) = (0.02, 0.02);
      (IN2 -=> Q) = (0.02, 0.02);
      (posedge S1 => (Q +: Q)) = (0.02, 0.02);
      (negedge S1 => (Q +: Q)) = (0.02, 0.02);
      (IN3 -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU4ILLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4:1 inverting Multiplexer
//   last modified by : XLICDD generated
//*****************************************************************

module MU4ILLX4 (IN0, IN1, IN2, IN3, Q, S0, S1);

   input   IN0, IN1, IN2, IN3, S0, S1;
   output  Q;
   wire n_0, n_1, n_2;

// logic section  
   u_mx2   i1 (n_0, IN0, IN1, S0);
   u_mx2   i2 (n_1, IN2, IN3, S0);
   u_mx2   i3 (n_2, n_0, n_1, S1);
   not     i4 (Q, n_2);
    

// timing section:
   specify

      (IN0 -=> Q) = (0.02, 0.02);
      (IN1 -=> Q) = (0.02, 0.02);
      (posedge S0 => (Q +: Q)) = (0.02, 0.02);
      (negedge S0 => (Q +: Q)) = (0.02, 0.02);
      (IN2 -=> Q) = (0.02, 0.02);
      (posedge S1 => (Q +: Q)) = (0.02, 0.02);
      (negedge S1 => (Q +: Q)) = (0.02, 0.02);
      (IN3 -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU4LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4:1 Multiplexer
//   last modified by : XLICDD generated
//*****************************************************************

module MU4LLX0 (IN0, IN1, IN2, IN3, Q, S0, S1);

   input   IN0, IN1, IN2, IN3, S0, S1;
   output  Q;
   wire n_0, n_1, n_2;

// logic section  
   u_mx2   i1 (n_0, IN0, IN1, S0);
   u_mx2   i2 (n_1, IN2, IN3, S0);
   u_mx2   i3 (n_2, n_0, n_1, S1);
   buf     i4 (Q, n_2);
    

// timing section:
   specify

      (IN0 +=> Q) = (0.02, 0.02);
      (IN1 +=> Q) = (0.02, 0.02);
      (posedge S0 => (Q +: Q)) = (0.02, 0.02);
      (negedge S0 => (Q +: Q)) = (0.02, 0.02);
      (IN2 +=> Q) = (0.02, 0.02);
      (posedge S1 => (Q +: Q)) = (0.02, 0.02);
      (negedge S1 => (Q +: Q)) = (0.02, 0.02);
      (IN3 +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU4LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4:1 Multiplexer
//   last modified by : XLICDD generated
//*****************************************************************

module MU4LLX1 (IN0, IN1, IN2, IN3, Q, S0, S1);

   input   IN0, IN1, IN2, IN3, S0, S1;
   output  Q;
   wire n_0, n_1, n_2;

// logic section  
   u_mx2   i1 (n_0, IN0, IN1, S0);
   u_mx2   i2 (n_1, IN2, IN3, S0);
   u_mx2   i3 (n_2, n_0, n_1, S1);
   buf     i4 (Q, n_2);
    

// timing section:
   specify

      (IN0 +=> Q) = (0.02, 0.02);
      (IN1 +=> Q) = (0.02, 0.02);
      (posedge S0 => (Q +: Q)) = (0.02, 0.02);
      (negedge S0 => (Q +: Q)) = (0.02, 0.02);
      (IN2 +=> Q) = (0.02, 0.02);
      (posedge S1 => (Q +: Q)) = (0.02, 0.02);
      (negedge S1 => (Q +: Q)) = (0.02, 0.02);
      (IN3 +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU4LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4:1 Multiplexer
//   last modified by : XLICDD generated
//*****************************************************************

module MU4LLX2 (IN0, IN1, IN2, IN3, Q, S0, S1);

   input   IN0, IN1, IN2, IN3, S0, S1;
   output  Q;
   wire n_0, n_1, n_2;

// logic section  
   u_mx2   i1 (n_0, IN0, IN1, S0);
   u_mx2   i2 (n_1, IN2, IN3, S0);
   u_mx2   i3 (n_2, n_0, n_1, S1);
   buf     i4 (Q, n_2);
    

// timing section:
   specify

      (IN0 +=> Q) = (0.02, 0.02);
      (IN1 +=> Q) = (0.02, 0.02);
      (posedge S0 => (Q +: Q)) = (0.02, 0.02);
      (negedge S0 => (Q +: Q)) = (0.02, 0.02);
      (IN2 +=> Q) = (0.02, 0.02);
      (posedge S1 => (Q +: Q)) = (0.02, 0.02);
      (negedge S1 => (Q +: Q)) = (0.02, 0.02);
      (IN3 +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MU4LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4:1 Multiplexer
//   last modified by : XLICDD generated
//*****************************************************************

module MU4LLX4 (IN0, IN1, IN2, IN3, Q, S0, S1);

   input   IN0, IN1, IN2, IN3, S0, S1;
   output  Q;
   wire n_0, n_1, n_2;

// logic section  
   u_mx2   i1 (n_0, IN0, IN1, S0);
   u_mx2   i2 (n_1, IN2, IN3, S0);
   u_mx2   i3 (n_2, n_0, n_1, S1);
   buf     i4 (Q, n_2);
    

// timing section:
   specify

      (IN0 +=> Q) = (0.02, 0.02);
      (IN1 +=> Q) = (0.02, 0.02);
      (posedge S0 => (Q +: Q)) = (0.02, 0.02);
      (negedge S0 => (Q +: Q)) = (0.02, 0.02);
      (IN2 +=> Q) = (0.02, 0.02);
      (posedge S1 => (Q +: Q)) = (0.02, 0.02);
      (negedge S1 => (Q +: Q)) = (0.02, 0.02);
      (IN3 +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA22LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA22LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_1;

// Function Q: !(!(A*B)*C)
   nand      i0  (n_1, A, B);
   nand      i2  (Q, n_1, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA22SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA22SLLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_1;

// Function Q: !(!(A*B)*C)
   nand      i0  (n_1, A, B);
   nand      i2  (Q, n_1, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA22SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA22SLLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_1;

// Function Q: !(!(A*B)*C)
   nand      i0  (n_1, A, B);
   nand      i2  (Q, n_1, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA22SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA22SLLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_1;

// Function Q: !(!(A*B)*C)
   nand      i0  (n_1, A, B);
   nand      i2  (Q, n_1, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA22SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA22SLLX6 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_1;

// Function Q: !(!(A*B)*C)
   nand      i0  (n_1, A, B);
   nand      i2  (Q, n_1, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2I1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA2I1LLX0 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2I1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA2I1LLX1 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2I1LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA2I1LLX2 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2I1LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA2I1LLX4 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2I1SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA2I1SLLX1 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;
 
// Function Q: !(!AN*B)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2I1SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA2I1SLLX2 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2I1SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA2I1SLLX4 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2I1SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA2I1SLLX6 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA2LLX0 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A*B)
   nand      i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA2LLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A*B)
   nand      i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA2LLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A*B)
   nand      i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA2LLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A*B)
   nand      i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA2SLLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A*B)
   nand      i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA2SLLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A*B)
   nand      i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA2SLLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A*B)
   nand      i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA2SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA2SLLX6 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A*B)
   nand      i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I1LLX0 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I1LLX1 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;


// Function Q: !(!AN*B*C)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I1LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I1LLX2 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I1LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I1LLX4 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I1SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I1SLLX1 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I1SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I1SLLX2 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I1SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I1SLLX4 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I1SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I1SLLX6 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I2LLX0 (AN, BN, C, Q);

   input     AN, BN, C;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I2LLX1 (AN, BN, C, Q);

   input     AN, BN, C;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I2LLX2 (AN, BN, C, Q);

   input     AN, BN, C;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3I2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA3I2LLX4 (AN, BN, C, Q);

   input     AN, BN, C;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA3LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A*B*C)
   nand      i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA3LLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A*B*C)
   nand      i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA3LLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A*B*C)
   nand      i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA3LLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A*B*C)
   nand      i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA3SLLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A*B*C)
   nand      i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA3SLLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A*B*C)
   nand      i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA3SLLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A*B*C)
   nand      i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA3SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA3SLLX6 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A*B*C)
   nand      i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I1LLX0 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I1LLX1 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I1LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I1LLX2 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I1LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I1LLX4 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I1SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I1SLLX1 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I1SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I1SLLX2 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I1SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I1SLLX4 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I1SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I1SLLX6 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I2LLX0 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I2LLX1 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I2LLX2 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I2LLX4 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I2SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 2 inverted inputs, speed
//                      optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I2SLLX1 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I2SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 2 inverted inputs, speed
//                      optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I2SLLX2 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I2SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 2 inverted inputs, speed
//                      optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I2SLLX4 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I2SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 2 inverted inputs, speed
//                      optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I2SLLX6 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I3LLX0 (AN, BN, CN, D, Q);

   input     AN, BN, CN, D;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I3LLX1 (AN, BN, CN, D, Q);

   input     AN, BN, CN, D;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I3LLX2 (AN, BN, CN, D, Q);

   input     AN, BN, CN, D;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4I3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA4I3LLX4 (AN, BN, CN, D, Q);

   input     AN, BN, CN, D;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA4LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A*B*C*D)
   nand      i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA4LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A*B*C*D)
   nand      i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA4LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A*B*C*D)
   nand      i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA4LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A*B*C*D)
   nand      i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 4-stocked n-channel transistors
//   last modified by : XLICDD generated
//****************************************************************************

module NA4SLLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A*B*C*D)
   nand      i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 4-stocked n-channel transistors
//   last modified by : XLICDD generated
//****************************************************************************

module NA4SLLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A*B*C*D)
   nand      i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 4-stocked n-channel transistors
//   last modified by : XLICDD generated
//****************************************************************************

module NA4SLLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A*B*C*D)
   nand      i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA4SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NAND with 4-stocked n-channel transistors
//   last modified by : XLICDD generated
//****************************************************************************

module NA4SLLX6 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A*B*C*D)
   nand      i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I1LLX0 (AN, B, C, D, E, Q);

   input     AN, B, C, D, E;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*B*C*D*E)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D, E);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I1LLX1 (AN, B, C, D, E, Q);

   input     AN, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D*E)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D, E);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I1LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I1LLX2 (AN, B, C, D, E, Q);

   input     AN, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D*E)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D, E);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I1LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I1LLX4 (AN, B, C, D, E, Q);

   input     AN, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D*E)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D, E);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I2LLX0 (AN, BN, C, D, E, Q);

   input     AN, BN, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D, E);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I2LLX1 (AN, BN, C, D, E, Q);

   input     AN, BN, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D, E);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I2LLX2 (AN, BN, C, D, E, Q);

   input     AN, BN, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D, E);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I2LLX4 (AN, BN, C, D, E, Q);

   input     AN, BN, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D, E);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I3LLX0 (AN, BN, CN, D, E, Q);

   input     AN, BN, CN, D, E;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D, E);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I3LLX1 (AN, BN, CN, D, E, Q);

   input     AN, BN, CN, D, E;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D, E);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I3LLX2 (AN, BN, CN, D, E, Q);

   input     AN, BN, CN, D, E;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D, E);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I3LLX4 (AN, BN, CN, D, E, Q);

   input     AN, BN, CN, D, E;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D, E);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I4LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I4LLX0 (AN, BN, CN, DN, E, Q);

   input     AN, BN, CN, DN, E;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN*!BN*!CN*!DN*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nand      i4  (Q, n_0, n_1, n_2, n_3, E);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I4LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I4LLX1 (AN, BN, CN, DN, E, Q);

   input     AN, BN, CN, DN, E;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN*!BN*!CN*!DN*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nand      i4  (Q, n_0, n_1, n_2, n_3, E);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I4LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I4LLX2 (AN, BN, CN, DN, E, Q);

   input     AN, BN, CN, DN, E;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN*!BN*!CN*!DN*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nand      i4  (Q, n_0, n_1, n_2, n_3, E);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5I4LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA5I4LLX4 (AN, BN, CN, DN, E, Q);

   input     AN, BN, CN, DN, E;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN*!BN*!CN*!DN*E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nand      i4  (Q, n_0, n_1, n_2, n_3, E);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA5LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: !(A*B*C*D*E)
   nand      i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA5LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: !(A*B*C*D*E)
   nand      i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA5LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: !(A*B*C*D*E)
   nand      i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA5LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA5LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: !(A*B*C*D*E)
   nand      i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I1LLX0 (AN, B, C, D, E, F, Q);

   input     AN, B, C, D, E, F;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D*E*F)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D, E, F);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I1LLX1 (AN, B, C, D, E, F, Q);

   input     AN, B, C, D, E, F;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D*E*F)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D, E, F);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I1LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I1LLX2 (AN, B, C, D, E, F, Q);

   input     AN, B, C, D, E, F;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D*E*F)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D, E, F);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I1LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I1LLX4 (AN, B, C, D, E, F, Q);

   input     AN, B, C, D, E, F;
   output    Q;
   wire n_0;

// Function Q: !(!AN*B*C*D*E*F)
   not       i0  (n_0, AN);
   nand      i1  (Q, n_0, B, C, D, E, F);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I2LLX0 (AN, BN, C, D, E, F, Q);

   input     AN, BN, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D, E, F);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I2LLX1 (AN, BN, C, D, E, F, Q);

   input     AN, BN, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D, E, F);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I2LLX2 (AN, BN, C, D, E, F, Q);

   input     AN, BN, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D, E, F);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I2LLX4 (AN, BN, C, D, E, F, Q);

   input     AN, BN, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN*!BN*C*D*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nand      i2  (Q, n_0, n_1, C, D, E, F);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I3LLX0 (AN, BN, CN, D, E, F, Q);

   input     AN, BN, CN, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D, E, F);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I3LLX1 (AN, BN, CN, D, E, F, Q);

   input     AN, BN, CN, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D, E, F);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I3LLX2 (AN, BN, CN, D, E, F, Q);

   input     AN, BN, CN, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D, E, F);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I3LLX4 (AN, BN, CN, D, E, F, Q);

   input     AN, BN, CN, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN*!BN*!CN*D*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nand      i3  (Q, n_0, n_1, n_2, D, E, F);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I4LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I4LLX0 (AN, BN, CN, DN, E, F, Q);

   input     AN, BN, CN, DN, E, F;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN*!BN*!CN*!DN*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nand      i4  (Q, n_0, n_1, n_2, n_3, E, F);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I4LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I4LLX1 (AN, BN, CN, DN, E, F, Q);

   input     AN, BN, CN, DN, E, F;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN*!BN*!CN*!DN*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nand      i4  (Q, n_0, n_1, n_2, n_3, E, F);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I4LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I4LLX2 (AN, BN, CN, DN, E, F, Q);

   input     AN, BN, CN, DN, E, F;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN*!BN*!CN*!DN*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nand      i4  (Q, n_0, n_1, n_2, n_3, E, F);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I4LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I4LLX4 (AN, BN, CN, DN, E, F, Q);

   input     AN, BN, CN, DN, E, F;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN*!BN*!CN*!DN*E*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nand      i4  (Q, n_0, n_1, n_2, n_3, E, F);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I5LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 5 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I5LLX0 (AN, BN, CN, DN, EN, F, Q);

   input     AN, BN, CN, DN, EN, F;
   output    Q;
   wire n_0,n_1,n_2,n_3,n_4;

// Function Q: !(!AN*!BN*!CN*!DN*!EN*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   not       i4  (n_4, EN);
   nand      i5  (Q, n_0, n_1, n_2, n_3, n_4, F);

// timing section:
   specify

      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);
      (EN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I5LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 5 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I5LLX1 (AN, BN, CN, DN, EN, F, Q);

   input     AN, BN, CN, DN, EN, F;
   output    Q;
   wire n_0,n_1,n_2,n_3,n_4;

// Function Q: !(!AN*!BN*!CN*!DN*!EN*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   not       i4  (n_4, EN);
   nand      i5  (Q, n_0, n_1, n_2, n_3, n_4, F);

// timing section:
   specify

      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);
      (EN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I5LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 5 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I5LLX2 (AN, BN, CN, DN, EN, F, Q);

   input     AN, BN, CN, DN, EN, F;
   output    Q;
   wire n_0,n_1,n_2,n_3,n_4;

// Function Q: !(!AN*!BN*!CN*!DN*!EN*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   not       i4  (n_4, EN);
   nand      i5  (Q, n_0, n_1, n_2, n_3, n_4, F);

// timing section:
   specify

      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);
      (EN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6I5LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND with 5 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NA6I5LLX4 (AN, BN, CN, DN, EN, F, Q);

   input     AN, BN, CN, DN, EN, F;
   output    Q;
   wire n_0,n_1,n_2,n_3,n_4;

// Function Q: !(!AN*!BN*!CN*!DN*!EN*F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   not       i4  (n_4, EN);
   nand      i5  (Q, n_0, n_1, n_2, n_3, n_4, F);

// timing section:
   specify

      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);
      (EN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA6LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: !(A*B*C*D*E*F)
   nand      i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA6LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: !(A*B*C*D*E*F)
   nand      i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA6LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: !(A*B*C*D*E*F)
   nand      i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NA6LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module NA6LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: !(A*B*C*D*E*F)
   nand      i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO22LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR into 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO22LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_1;

// Function Q: !(!(A+B)+C)
   nor       i0  (n_1, A, B);
   nor       i2  (Q, n_1, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO22SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO22SLLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_1;

// Function Q: !(!(A+B)+C)
   nor       i0  (n_1, A, B);
   nor       i2  (Q, n_1, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO22SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO22SLLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_1;

// Function Q: !(!(A+B)+C)
   nor       i0  (n_1, A, B);
   nor       i2  (Q, n_1, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO22SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO22SLLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_1;

// Function Q: !(!(A+B)+C)
   nor       i0  (n_1, A, B);
   nor       i2  (Q, n_1, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO22SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR into 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO22SLLX6 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_1;

// Function Q: !(!(A+B)+C)
   nor       i0  (n_1, A, B);
   nor       i2  (Q, n_1, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2I1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO2I1LLX0 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2I1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO2I1LLX1 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2I1LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO2I1LLX2 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2I1LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO2I1LLX4 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2I1SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO2I1SLLX1 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2I1SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO2I1SLLX2 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2I1SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO2I1SLLX4 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2I1SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO2I1SLLX6 (AN, B, Q);

   input     AN, B;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO2LLX0 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A+B)
   nor       i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO2LLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A+B)
   nor       i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO2LLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A+B)
   nor       i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO2LLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A+B)
   nor       i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO2SLLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A+B)
   nor       i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO2SLLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A+B)
   nor       i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO2SLLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A+B)
   nor       i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO2SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO2SLLX6 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: !(A+B)
   nor       i0  (Q, A, B);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I1LLX0 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I1LLX1 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I1LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I1LLX2 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I1LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I1LLX4 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I1SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I1SLLX1 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I1SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I1SLLX2 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I1SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I1SLLX4 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I1SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 1 inverted input, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I1SLLX6 (AN, B, C, Q);

   input     AN, B, C;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I2LLX0 (AN, BN, C, Q);

   input     AN, BN, C;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I2LLX1 (AN, BN, C, Q);

   input     AN, BN, C;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I2LLX2 (AN, BN, C, Q);

   input     AN, BN, C;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3I2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO3I2LLX4 (AN, BN, C, Q);

   input     AN, BN, C;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO3LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A+B+C)
   nor       i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO3LLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A+B+C)
   nor       i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO3LLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A+B+C)
   nor       i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO3LLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A+B+C)
   nor       i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO3SLLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A+B+C)
   nor       i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO3SLLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A+B+C)
   nor       i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO3SLLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A+B+C)
   nor       i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO3SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input NOR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module NO3SLLX6 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: !(A+B+C)
   nor       i0  (Q, A, B, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I1LLX0 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I1LLX1 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I1LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I1LLX2 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I1LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I1LLX4 (AN, B, C, D, Q);

   input     AN, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I2LLX0 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I2LLX1 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I2LLX2 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I2LLX4 (AN, BN, C, D, Q);

   input     AN, BN, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I3LLX0 (AN, BN, CN, D, Q);

   input     AN, BN, CN, D;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I3LLX1 (AN, BN, CN, D, Q);

   input     AN, BN, CN, D;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I3LLX2 (AN, BN, CN, D, Q);

   input     AN, BN, CN, D;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4I3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO4I3LLX4 (AN, BN, CN, D, Q);

   input     AN, BN, CN, D;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO4LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A+B+C+D)
   nor       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO4LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A+B+C+D)
   nor       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO4LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A+B+C+D)
   nor       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO4LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO4LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: !(A+B+C+D)
   nor       i0  (Q, A, B, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I1LLX0 (AN, B, C, D, E, Q);

   input     AN, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D+E)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D, E);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I1LLX1 (AN, B, C, D, E, Q);

   input     AN, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D+E)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D, E);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I1LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I1LLX2 (AN, B, C, D, E, Q);

   input     AN, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D+E)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D, E);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I1LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I1LLX4 (AN, B, C, D, E, Q);

   input     AN, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D+E)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D, E);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I2LLX0 (AN, BN, C, D, E, Q);

   input     AN, BN, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D, E);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I2LLX1 (AN, BN, C, D, E, Q);

   input     AN, BN, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D, E);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I2LLX2 (AN, BN, C, D, E, Q);

   input     AN, BN, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D, E);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 2 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I2LLX4 (AN, BN, C, D, E, Q);

   input     AN, BN, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D, E);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I3LLX0 (AN, BN, CN, D, E, Q);

   input     AN, BN, CN, D, E;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D, E);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I3LLX1 (AN, BN, CN, D, E, Q);

   input     AN, BN, CN, D, E;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D, E);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I3LLX2 (AN, BN, CN, D, E, Q);

   input     AN, BN, CN, D, E;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D, E);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NAND with 3 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I3LLX4 (AN, BN, CN, D, E, Q);

   input     AN, BN, CN, D, E;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D, E);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I4LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I4LLX0 (AN, BN, CN, DN, E, Q);

   input     AN, BN, CN, DN, E;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN+!BN+!CN+!DN+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nor       i4  (Q, n_0, n_1, n_2, n_3, E);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I4LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I4LLX1 (AN, BN, CN, DN, E, Q);

   input     AN, BN, CN, DN, E;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN+!BN+!CN+!DN+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nor       i4  (Q, n_0, n_1, n_2, n_3, E);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I4LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I4LLX2 (AN, BN, CN, DN, E, Q);

   input     AN, BN, CN, DN, E;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN+!BN+!CN+!DN+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nor       i4  (Q, n_0, n_1, n_2, n_3, E);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5I4LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR with 4 inverted inputs
//   last modified by : XLICDD generated
//****************************************************************************

module NO5I4LLX4 (AN, BN, CN, DN, E, Q);

   input     AN, BN, CN, DN, E;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN+!BN+!CN+!DN+E)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nor       i4  (Q, n_0, n_1, n_2, n_3, E);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO5LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: !(A+B+C+D+E)
   nor       i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO5LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: !(A+B+C+D+E)
   nor       i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO5LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: !(A+B+C+D+E)
   nor       i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO5LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO5LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: !(A+B+C+D+E)
   nor       i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I1LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I1LLX0 (AN, B, C, D, E, F, Q);

   input     AN, B, C, D, E, F;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D+E+F)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D, E, F);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I1LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I1LLX1 (AN, B, C, D, E, F, Q);

   input     AN, B, C, D, E, F;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D+E+F)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D, E, F);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I1LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I1LLX2 (AN, B, C, D, E, F, Q);

   input     AN, B, C, D, E, F;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D+E+F)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D, E, F);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I1LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 1 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I1LLX4 (AN, B, C, D, E, F, Q);

   input     AN, B, C, D, E, F;
   output    Q;
   wire n_0;

// Function Q: !(!AN+B+C+D+E+F)
   not       i0  (n_0, AN);
   nor       i1  (Q, n_0, B, C, D, E, F);

// timing section:
   specify

      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I2LLX0 (AN, BN, C, D, E, F, Q);

   input     AN, BN, C, D, E, F;
   output    Q;
   wire n_0;
   wire n_1;

// Function Q: !(!AN+!BN+C+D+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D, E, F);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I2LLX1 (AN, BN, C, D, E, F, Q);

   input     AN, BN, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D, E, F);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I2LLX2 (AN, BN, C, D, E, F, Q);

   input     AN, BN, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D, E, F);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 2 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I2LLX4 (AN, BN, C, D, E, F, Q);

   input     AN, BN, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !(!AN+!BN+C+D+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   nor       i2  (Q, n_0, n_1, C, D, E, F);

// timing section:
   specify

      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 3 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I3LLX0 (AN, BN, CN, D, E, F, Q);

   input     AN, BN, CN, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D, E, F);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 3 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I3LLX1 (AN, BN, CN, D, E, F, Q);

   input     AN, BN, CN, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D, E, F);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 3 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I3LLX2 (AN, BN, CN, D, E, F, Q);

   input     AN, BN, CN, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D, E, F);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 3 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I3LLX4 (AN, BN, CN, D, E, F, Q);

   input     AN, BN, CN, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !(!AN+!BN+!CN+D+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   nor       i3  (Q, n_0, n_1, n_2, D, E, F);

// timing section:
   specify

      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I4LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 4 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I4LLX0 (AN, BN, CN, DN, E, F, Q);

   input     AN, BN, CN, DN, E, F;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN+!BN+!CN+!DN+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nor       i4  (Q, n_0, n_1, n_2, n_3, E, F);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I4LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 4 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I4LLX1 (AN, BN, CN, DN, E, F, Q);

   input     AN, BN, CN, DN, E, F;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN+!BN+!CN+!DN+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nor       i4  (Q, n_0, n_1, n_2, n_3, E, F);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I4LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 4 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I4LLX2 (AN, BN, CN, DN, E, F, Q);

   input     AN, BN, CN, DN, E, F;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN+!BN+!CN+!DN+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nor       i4  (Q, n_0, n_1, n_2, n_3, E, F);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I4LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 4 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I4LLX4 (AN, BN, CN, DN, E, F, Q);

   input     AN, BN, CN, DN, E, F;
   output    Q;
   wire n_0,n_1,n_2,n_3;

// Function Q: !(!AN+!BN+!CN+!DN+E+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   nor       i4  (Q, n_0, n_1, n_2, n_3, E, F);

// timing section:
   specify

      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I5LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 5 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I5LLX0 (AN, BN, CN, DN, EN, F, Q);

   input     AN, BN, CN, DN, EN, F;
   output    Q;
   wire n_0,n_1,n_2,n_3,n_4;

// Function Q: !(!AN+!BN+!CN+!DN+!EN+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   not       i4  (n_4, EN);
   nor       i5  (Q, n_0, n_1, n_2, n_3, n_4, F);

// timing section:
   specify

      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);
      (EN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I5LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 5 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I5LLX1 (AN, BN, CN, DN, EN, F, Q);

   input     AN, BN, CN, DN, EN, F;
   output    Q;
   wire n_0,n_1,n_2,n_3,n_4;

// Function Q: !(!AN+!BN+!CN+!DN+!EN+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   not       i4  (n_4, EN);
   nor       i5  (Q, n_0, n_1, n_2, n_3, n_4, F);

// timing section:
   specify

      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);
      (EN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I5LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 5 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I5LLX2 (AN, BN, CN, DN, EN, F, Q);

   input     AN, BN, CN, DN, EN, F;
   output    Q;
   wire n_0,n_1,n_2,n_3,n_4;

// Function Q: !(!AN+!BN+!CN+!DN+!EN+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   not       i4  (n_4, EN);
   nor       i5  (Q, n_0, n_1, n_2, n_3, n_4, F);

// timing section:
   specify

      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);
      (EN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6I5LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR with 5 inverted input
//   last modified by : XLICDD generated
//****************************************************************************

module NO6I5LLX4 (AN, BN, CN, DN, EN, F, Q);

   input     AN, BN, CN, DN, EN, F;
   output    Q;
   wire n_0,n_1,n_2,n_3,n_4;

// Function Q: !(!AN+!BN+!CN+!DN+!EN+F)
   not       i0  (n_0, AN);
   not       i1  (n_1, BN);
   not       i2  (n_2, CN);
   not       i3  (n_3, DN);
   not       i4  (n_4, EN);
   nor       i5  (Q, n_0, n_1, n_2, n_3, n_4, F);

// timing section:
   specify

      (F -=> Q) = (0.02, 0.02);
      (AN +=> Q) = (0.02, 0.02);
      (BN +=> Q) = (0.02, 0.02);
      (CN +=> Q) = (0.02, 0.02);
      (DN +=> Q) = (0.02, 0.02);
      (EN +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO6LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: !(A+B+C+D+E+F)
   nor       i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO6LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: !(A+B+C+D+E+F)
   nor       i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO6LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: !(A+B+C+D+E+F)
   nor       i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : NO6LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input NOR
//   last modified by : XLICDD generated
//****************************************************************************

module NO6LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: !(A+B+C+D+E+F)
   nor       i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA211LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA211LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C*D)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA211LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA211LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C*D)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA211LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA211LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C*D)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA211LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA211LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C*D)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA21LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA21LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA21LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA21LLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA21LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA21LLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA21LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA21LLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA21SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA21SLLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA21SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA21SLLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA21SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA21SLLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA21SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA21SLLX6 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: ((A+B)*C)
   or        i0  (n_0, A, B);
   and       i1  (Q, n_0, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA221LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA221LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D)*E)
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (A +=> Q) =
                   (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (B +=> Q) =
                   (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0 && E == 1'b1)) (C +=> Q) =
                   (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && E == 1'b1)) (D +=> Q) =
                   (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b1 && C == 1'b1
                   && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0)) (E +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA221LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA221LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D)*E)
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (A +=> Q) =
                   (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (B +=> Q) =
                   (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0 && E == 1'b1)) (C +=> Q) =
                   (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && E == 1'b1)) (D +=> Q) =
                   (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b1 && C == 1'b1
                   && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0)) (E +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA221LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA221LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D)*E)
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (A +=> Q) =
                   (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (B +=> Q) =
                   (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0 && E == 1'b1)) (C +=> Q) =
                   (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && E == 1'b1)) (D +=> Q) =
                   (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b1 && C == 1'b1
                   && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0)) (E +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA221LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA221LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D)*E)
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (A +=> Q) =
                   (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (B +=> Q) =
                   (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0 && E == 1'b1)) (C +=> Q) =
                   (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && E == 1'b1)) (D +=> Q) =
                   (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b1 && C == 1'b1
                   && D == 1'b0)) (E +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0)) (E +=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA222LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2 Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA222LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: ((A+B)*(C+D)*(E+F))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   or        i2  (n_2, E, F);
   and       i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && F == 1'b0)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA222LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2 Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA222LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: ((A+B)*(C+D)*(E+F))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   or        i2  (n_2, E, F);
   and       i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && F == 1'b0)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA222LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2 Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA222LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: ((A+B)*(C+D)*(E+F))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   or        i2  (n_2, E, F);
   and       i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && F == 1'b0)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA222LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2 Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA222LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: ((A+B)*(C+D)*(E+F))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   or        i2  (n_2, E, F);
   and       i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && F == 1'b0)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA22LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA22LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA22LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA22LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA22LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA22LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA22LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA22LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA22SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA22SLLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA22SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA22SLLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA22SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA22SLLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA22SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA22SLLX6 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA311LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA311LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D*E)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA311LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA311LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D*E)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA311LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA311LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D*E)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA311LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA311LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D*E)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA31LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA31LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA31LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA31LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA31LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA31LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA31LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA31LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA31SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA31SLLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA31SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA31SLLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA31SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA31SLLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA31SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input AND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OA31SLLX6 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: ((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   and       i1  (Q, n_0, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA321LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA321LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E)*F)
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   and       i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b1) || (A == 1'b0
                   && B == 1'b1 && C == 1'b0 && E == 1'b1) || (A == 1'b0 && B
                   == 1'b0 && C == 1'b1 && E == 1'b0) || (C == 1'b1 && D ==
                   1'b1 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0) || (B == 1'b0
                   && C == 1'b0 && D == 1'b0 && E == 1'b1)) (F +=> Q) = (0.02,
                   0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0))
                   (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA321LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA321LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E)*F)
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   and       i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b1) || (A == 1'b0
                   && B == 1'b1 && C == 1'b0 && E == 1'b1) || (A == 1'b0 && B
                   == 1'b0 && C == 1'b1 && E == 1'b0) || (C == 1'b1 && D ==
                   1'b1 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0) || (B == 1'b0
                   && C == 1'b0 && D == 1'b0 && E == 1'b1)) (F +=> Q) = (0.02,
                   0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0))
                   (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA321LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA321LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E)*F)
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   and       i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b1) || (A == 1'b0
                   && B == 1'b1 && C == 1'b0 && E == 1'b1) || (A == 1'b0 && B
                   == 1'b0 && C == 1'b1 && E == 1'b0) || (C == 1'b1 && D ==
                   1'b1 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0) || (B == 1'b0
                   && C == 1'b0 && D == 1'b0 && E == 1'b1)) (F +=> Q) = (0.02,
                   0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0))
                   (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA321LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 3-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA321LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E)*F)
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   and       i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b1)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b1)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b1) || (A == 1'b0
                   && B == 1'b1 && C == 1'b0 && E == 1'b1) || (A == 1'b0 && B
                   == 1'b0 && C == 1'b1 && E == 1'b0) || (C == 1'b1 && D ==
                   1'b1 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0) || (B == 1'b0
                   && C == 1'b0 && D == 1'b0 && E == 1'b1)) (F +=> Q) = (0.02,
                   0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0))
                   (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA32LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA32LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA32LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA32LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA32LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA32LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA32LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA32LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0)) (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA33LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA33LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E+F))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E, F);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && F == 1'b0)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && F == 1'b0)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && F == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b0)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA33LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA33LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E+F))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E, F);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && F == 1'b0)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && F == 1'b0)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && F == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b0)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA33LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA33LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E+F))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E, F);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && F == 1'b0)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && F == 1'b0)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && F == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b0)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OA33LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input OR into 2-Input AND
//   last modified by : XLICDD generated
//****************************************************************************

module OA33LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: ((A+B+C)*(D+E+F))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E, F);
   and       i2  (Q, n_0, n_1);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && F == 1'b0)) (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && F == 1'b0)) (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && F == 1'b0)) (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b0)) (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b0)) (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && E == 1'b0)) (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON211LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON211LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C*D)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON211LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON211LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C*D)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON211LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON211LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C*D)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON211LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON211LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C*D)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON21LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON21LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON21LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON21LLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON21LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON21LLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON21LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON21LLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON21SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON21SLLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON21SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON21SLLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON21SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON21SLLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON21SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON21SLLX6 (A, B, C, Q);

   input     A, B, C;
   output    Q;
   wire n_0;

// Function Q: !((A+B)*C)
   or        i0  (n_0, A, B);
   nand      i1  (Q, n_0, C);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON221LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON221LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D)*E)
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (A -=> Q) =
                   (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (B -=> Q) =
                   (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0 && E == 1'b1)) (C -=> Q) =
                   (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && E == 1'b1)) (D -=> Q) =
                   (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b1 && C == 1'b1
                   && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0)) (E -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON221LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON221LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D)*E)
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (A -=> Q) =
                   (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (B -=> Q) =
                   (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0 && E == 1'b1)) (C -=> Q) =
                   (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && E == 1'b1)) (D -=> Q) =
                   (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b1 && C == 1'b1
                   && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0)) (E -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON221LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON221LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D)*E)
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (A -=> Q) =
                   (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (B -=> Q) =
                   (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0 && E == 1'b1)) (C -=> Q) =
                   (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && E == 1'b1)) (D -=> Q) =
                   (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b1 && C == 1'b1
                   && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0)) (E -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON221LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON221LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D)*E)
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (A -=> Q) =
                   (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b1)) (B -=> Q) =
                   (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0 && E == 1'b1)) (C -=> Q) =
                   (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && E == 1'b1)) (D -=> Q) =
                   (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && D == 1'b1) || (A == 1'b1 && B == 1'b1 && C == 1'b1
                   && D == 1'b0)) (E -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b1 && D == 1'b0)) (E -=> Q) =
                   (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON222LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2 Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON222LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !((A+B)*(C+D)*(E+F))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   or        i2  (n_2, E, F);
   nand      i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && F == 1'b0)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON222LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2 Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON222LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !((A+B)*(C+D)*(E+F))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   or        i2  (n_2, E, F);
   nand      i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && F == 1'b0)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON222LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2 Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON222LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !((A+B)*(C+D)*(E+F))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   or        i2  (n_2, E, F);
   nand      i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && F == 1'b0)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON222LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3x2 Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON222LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1,n_2;

// Function Q: !((A+B)*(C+D)*(E+F))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   or        i2  (n_2, E, F);
   nand      i3  (Q, n_0, n_1, n_2);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b1 && D == 1'b0 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && D == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && F == 1'b0)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b1 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON22LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON22LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON22LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON22LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON22LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON22LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON22LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON22LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON22SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON22SLLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON22SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON22SLLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON22SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON22SLLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON22SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x2-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON22SLLX6 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B)*(C+D))
   or        i0  (n_0, A, B);
   or        i1  (n_1, C, D);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b1)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON311LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON311LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*D*E)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON311LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON311LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D*E)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON311LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON311LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D*E)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON311LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON311LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D*E)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D, E);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b1)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON31LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON31LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON31LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON31LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON31LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON31LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON31LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON31LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON31SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON31SLLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON31SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON31SLLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON31SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON31SLLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON31SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR into 2-Input NAND, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module ON31SLLX6 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;
   wire n_0;

// Function Q: !((A+B+C)*D)
   or        i0  (n_0, A, B, C);
   nand      i1  (Q, n_0, D);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0)) (D -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON321LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON321LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E)*F)
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   nand      i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b1) || (A == 1'b0
                   && B == 1'b1 && C == 1'b0 && E == 1'b1) || (A == 1'b0 && B
                   == 1'b0 && C == 1'b1 && E == 1'b0) || (C == 1'b1 && D ==
                   1'b1 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0) || (B == 1'b0
                   && C == 1'b0 && D == 1'b0 && E == 1'b1)) (F -=> Q) = (0.02,
                   0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0))
                   (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON321LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON321LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E)*F)
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   nand      i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b1) || (A == 1'b0
                   && B == 1'b1 && C == 1'b0 && E == 1'b1) || (A == 1'b0 && B
                   == 1'b0 && C == 1'b1 && E == 1'b0) || (C == 1'b1 && D ==
                   1'b1 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0) || (B == 1'b0
                   && C == 1'b0 && D == 1'b0 && E == 1'b1)) (F -=> Q) = (0.02,
                   0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0))
                   (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON321LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON321LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E)*F)
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   nand      i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b1) || (A == 1'b0
                   && B == 1'b1 && C == 1'b0 && E == 1'b1) || (A == 1'b0 && B
                   == 1'b0 && C == 1'b1 && E == 1'b0) || (C == 1'b1 && D ==
                   1'b1 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0) || (B == 1'b0
                   && C == 1'b0 && D == 1'b0 && E == 1'b1)) (F -=> Q) = (0.02,
                   0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0))
                   (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON321LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 3-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON321LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E)*F)
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   nand      i2  (Q, n_0, n_1, F);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && D == 1'b1 && E == 1'b0 && F == 1'b1))
                   (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b1)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b1)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b1) || (A == 1'b0
                   && B == 1'b1 && C == 1'b0 && E == 1'b1) || (A == 1'b0 && B
                   == 1'b0 && C == 1'b1 && E == 1'b0) || (C == 1'b1 && D ==
                   1'b1 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);
         if ((B == 1'b1 && C == 1'b0 && D == 1'b1 && E == 1'b0) || (B == 1'b0
                   && C == 1'b0 && D == 1'b0 && E == 1'b1)) (F -=> Q) = (0.02,
                   0.02);
         if ((A == 1'b1 && B == 1'b0 && C == 1'b0 && D == 1'b1 && E == 1'b0))
                   (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON32LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON32LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON32LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON32LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON32LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON32LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON32LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, 2-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON32LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0)) (E -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON33LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON33LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E+F))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E, F);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && F == 1'b0)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && F == 1'b0)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && F == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b0)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON33LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON33LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E+F))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E, F);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && F == 1'b0)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && F == 1'b0)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && F == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b0)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON33LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON33LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E+F))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E, F);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && F == 1'b0)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && F == 1'b0)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && F == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b0)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ON33LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2x3-Input OR into 2-Input NAND
//   last modified by : XLICDD generated
//****************************************************************************

module ON33LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;
   wire n_0,n_1;

// Function Q: !((A+B+C)*(D+E+F))
   or        i0  (n_0, A, B, C);
   or        i1  (n_1, D, E, F);
   nand      i2  (Q, n_0, n_1);

// timing section:
   specify

      (A -=> Q) = (0.02, 0.02);
         if ((B == 1'b0 && C == 1'b0 && F == 1'b0)) (A -=> Q) = (0.02, 0.02);
      (B -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && C == 1'b0 && F == 1'b0)) (B -=> Q) = (0.02, 0.02);
      (C -=> Q) = (0.02, 0.02);
         if ((A == 1'b0 && B == 1'b0 && F == 1'b0)) (C -=> Q) = (0.02, 0.02);
      (D -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && E == 1'b0 && F == 1'b0)) (D -=> Q) = (0.02, 0.02);
      (E -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && F == 1'b0)) (E -=> Q) = (0.02, 0.02);
      (F -=> Q) = (0.02, 0.02);
         if ((C == 1'b0 && D == 1'b0 && E == 1'b0)) (F -=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR2LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR2LLX0 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A+B)
   or        i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR2LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR2LLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A+B)
   or        i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR2LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR2LLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A+B)
   or        i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR2LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR2LLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A+B)
   or        i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR2SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OR2SLLX1 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A+B)
   or        i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR2SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OR2SLLX2 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A+B)
   or        i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR2SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OR2SLLX4 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A+B)
   or        i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR2SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 2-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OR2SLLX6 (A, B, Q);

   input     A, B;
   output    Q;

// Function Q: (A+B)
   or        i0  (Q, A, B);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR3LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR3LLX0 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A+B+C)
   or        i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR3LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR3LLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A+B+C)
   or        i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR3LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR3LLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A+B+C)
   or        i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR3LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR3LLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A+B+C)
   or        i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR3SLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OR3SLLX1 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A+B+C)
   or        i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR3SLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OR3SLLX2 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A+B+C)
   or        i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR3SLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OR3SLLX4 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A+B+C)
   or        i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR3SLLX6
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 3-Input OR, speed optimized
//   last modified by : XLICDD generated
//****************************************************************************

module OR3SLLX6 (A, B, C, Q);

   input     A, B, C;
   output    Q;

// Function Q: (A+B+C)
   or        i0  (Q, A, B, C);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR4LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR4LLX0 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A+B+C+D)
   or        i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR4LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR4LLX1 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A+B+C+D)
   or        i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR4LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR4LLX2 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A+B+C+D)
   or        i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR4LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 4-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR4LLX4 (A, B, C, D, Q);

   input     A, B, C, D;
   output    Q;

// Function Q: (A+B+C+D)
   or        i0  (Q, A, B, C, D);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR5LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR5LLX0 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: (A+B+C+D+E)
   or        i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR5LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR5LLX1 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: (A+B+C+D+E)
   or        i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR5LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR5LLX2 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: (A+B+C+D+E)
   or        i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR5LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 5-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR5LLX4 (A, B, C, D, E, Q);

   input     A, B, C, D, E;
   output    Q;

// Function Q: (A+B+C+D+E)
   or        i0  (Q, A, B, C, D, E);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR6LLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR6LLX0 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: (A+B+C+D+E+F)
   or        i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR6LLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR6LLX1 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: (A+B+C+D+E+F)
   or        i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR6LLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR6LLX2 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: (A+B+C+D+E+F)
   or        i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : OR6LLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : 6-Input OR
//   last modified by : XLICDD generated
//****************************************************************************

module OR6LLX4 (A, B, C, D, E, F, Q);

   input     A, B, C, D, E, F;
   output    Q;

// Function Q: (A+B+C+D+E+F)
   or        i0  (Q, A, B, C, D, E, F);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);
      (B +=> Q) = (0.02, 0.02);
      (C +=> Q) = (0.02, 0.02);
      (D +=> Q) = (0.02, 0.02);
      (E +=> Q) = (0.02, 0.02);
      (F +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFQLLX0 (CN, D, Q, SD, SE);

   input     CN, D, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, d_clock_i, d_dat_i,
                   c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   not       i8  (c_SH_D, delay_SE);
   buf       i9  (c_SH_SD, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   not       i8  (c_SH_D, SE);
   buf       i9  (c_SH_SD, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFQLLX1 (CN, D, Q, SD, SE);

   input     CN, D, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, d_clock_i, d_dat_i,
                   c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   not       i8  (c_SH_D, delay_SE);
   buf       i9  (c_SH_SD, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   not       i8  (c_SH_D, SE);
   buf       i9  (c_SH_SD, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFQLLX2 (CN, D, Q, SD, SE);

   input     CN, D, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, d_clock_i, d_dat_i,
                   c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   not       i8  (c_SH_D, delay_SE);
   buf       i9  (c_SH_SD, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   not       i8  (c_SH_D, SE);
   buf       i9  (c_SH_SD, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFQLLX4 (CN, D, Q, SD, SE);

   input     CN, D, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, d_clock_i, d_dat_i,
                   c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   not       i8  (c_SH_D, delay_SE);
   buf       i9  (c_SH_SD, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   not       i8  (c_SH_D, SE);
   buf       i9  (c_SH_SD, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRQLLX0 (CN, D, Q, RN, SD, SE);

   input     CN, D, RN, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, d_clock_i,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, RN, 1'b1, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, RN, 1'b1);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRQLLX1 (CN, D, Q, RN, SD, SE);

   input     CN, D, RN, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, d_clock_i,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, RN, 1'b1, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, RN, 1'b1);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRQLLX2 (CN, D, Q, RN, SD, SE);

   input     CN, D, RN, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, d_clock_i,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, RN, 1'b1, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, RN, 1'b1);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRQLLX4 (CN, D, Q, RN, SD, SE);

   input     CN, D, RN, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, d_clock_i,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, RN, 1'b1, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, RN, 1'b1);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset, Set and
//                      Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRSQLLX0 (CN, D, Q, RN, SD, SE, SN);

   input     CN, D, RN, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_clock_i, d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i7  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i8  (IQ, d_dat_i, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   buf       i9  (Q, IQ);

   checkrs   i10 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i7  (dat_i, D, SD, SE);
   u1_fd5    i8  (IQ, dat_i, clock_i, RN, SN, NOTIFY_REG);
   buf       i9  (Q, IQ);

   checkrs   i10 (c_SH_dat_i, RN, SN);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset, Set and
//                      Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRSQLLX1 (CN, D, Q, RN, SD, SE, SN);

   input     CN, D, RN, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_clock_i, d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i7  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i8  (IQ, d_dat_i, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   buf       i9  (Q, IQ);

   checkrs   i10 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i7  (dat_i, D, SD, SE);
   u1_fd5    i8  (IQ, dat_i, clock_i, RN, SN, NOTIFY_REG);
   buf       i9  (Q, IQ);

   checkrs   i10 (c_SH_dat_i, RN, SN);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset, Set and
//                      Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRSQLLX2 (CN, D, Q, RN, SD, SE, SN);

   input     CN, D, RN, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_clock_i, d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i7  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i8  (IQ, d_dat_i, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   buf       i9  (Q, IQ);

   checkrs   i10 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i7  (dat_i, D, SD, SE);
   u1_fd5    i8  (IQ, dat_i, clock_i, RN, SN, NOTIFY_REG);
   buf       i9  (Q, IQ);

   checkrs   i10 (c_SH_dat_i, RN, SN);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Reset, Set and
//                      Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRSQLLX4 (CN, D, Q, RN, SD, SE, SN);

   input     CN, D, RN, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_clock_i, d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i7  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i8  (IQ, d_dat_i, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   buf       i9  (Q, IQ);

   checkrs   i10 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i7  (dat_i, D, SD, SE);
   u1_fd5    i8  (IQ, dat_i, clock_i, RN, SN, NOTIFY_REG);
   buf       i9  (Q, IQ);

   checkrs   i10 (c_SH_dat_i, RN, SN);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset, Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRSLLX0 (CN, D, Q, QN, RN, SD, SE, SN);

   input     CN, D, RN, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_clock_i, d_dat_i, IQN, qin_and_set, c_SH_d_dat_i, SE_not,
                   c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i7  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i8  (IQ, d_dat_i, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   not       i9  (IQN, IQ);
   and       i10 (qin_and_set, IQN, delay_SN);
   buf       i11 (Q, IQ);
   buf       i12 (QN, qin_and_set);

   checkrs   i13 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i14 (SE_not, delay_SE);
   and       i15 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i16 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, qin_and_set, c_SH_dat_i, SE_not, c_SH_D,
                   c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i7  (dat_i, D, SD, SE);
   u1_fd5    i8  (IQ, dat_i, clock_i, RN, SN, NOTIFY_REG);
   not       i9  (IQN, IQ);
   and       i10 (qin_and_set, IQN, SN);
   buf       i11 (Q, IQ);
   buf       i12 (QN, qin_and_set);

   checkrs   i13 (c_SH_dat_i, RN, SN);
   not       i14 (SE_not, SE);
   and       i15 (c_SH_D, c_SH_dat_i, SE_not);
   and       i16 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset, Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRSLLX1 (CN, D, Q, QN, RN, SD, SE, SN);

   input     CN, D, RN, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_clock_i, d_dat_i, IQN, qin_and_set, c_SH_d_dat_i, SE_not,
                   c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i7  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i8  (IQ, d_dat_i, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   not       i9  (IQN, IQ);
   and       i10 (qin_and_set, IQN, delay_SN);
   buf       i11 (Q, IQ);
   buf       i12 (QN, qin_and_set);

   checkrs   i13 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i14 (SE_not, delay_SE);
   and       i15 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i16 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, qin_and_set, c_SH_dat_i, SE_not, c_SH_D,
                   c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i7  (dat_i, D, SD, SE);
   u1_fd5    i8  (IQ, dat_i, clock_i, RN, SN, NOTIFY_REG);
   not       i9  (IQN, IQ);
   and       i10 (qin_and_set, IQN, SN);
   buf       i11 (Q, IQ);
   buf       i12 (QN, qin_and_set);

   checkrs   i13 (c_SH_dat_i, RN, SN);
   not       i14 (SE_not, SE);
   and       i15 (c_SH_D, c_SH_dat_i, SE_not);
   and       i16 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset, Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRSLLX2 (CN, D, Q, QN, RN, SD, SE, SN);

   input     CN, D, RN, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_clock_i, d_dat_i, IQN, qin_and_set, c_SH_d_dat_i, SE_not,
                   c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i7  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i8  (IQ, d_dat_i, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   not       i9  (IQN, IQ);
   and       i10 (qin_and_set, IQN, delay_SN);
   buf       i11 (Q, IQ);
   buf       i12 (QN, qin_and_set);

   checkrs   i13 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i14 (SE_not, delay_SE);
   and       i15 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i16 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, qin_and_set, c_SH_dat_i, SE_not, c_SH_D,
                   c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i7  (dat_i, D, SD, SE);
   u1_fd5    i8  (IQ, dat_i, clock_i, RN, SN, NOTIFY_REG);
   not       i9  (IQN, IQ);
   and       i10 (qin_and_set, IQN, SN);
   buf       i11 (Q, IQ);
   buf       i12 (QN, qin_and_set);

   checkrs   i13 (c_SH_dat_i, RN, SN);
   not       i14 (SE_not, SE);
   and       i15 (c_SH_D, c_SH_dat_i, SE_not);
   and       i16 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset, Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRSLLX4 (CN, D, Q, QN, RN, SD, SE, SN);

   input     CN, D, RN, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_clock_i, d_dat_i, IQN, qin_and_set, c_SH_d_dat_i, SE_not,
                   c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i7  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i8  (IQ, d_dat_i, d_clock_i, delay_RN, delay_SN, NOTIFY_REG);
   not       i9  (IQN, IQ);
   and       i10 (qin_and_set, IQN, delay_SN);
   buf       i11 (Q, IQ);
   buf       i12 (QN, qin_and_set);

   checkrs   i13 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i14 (SE_not, delay_SE);
   and       i15 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i16 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, qin_and_set, c_SH_dat_i, SE_not, c_SH_D,
                   c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i7  (dat_i, D, SD, SE);
   u1_fd5    i8  (IQ, dat_i, clock_i, RN, SN, NOTIFY_REG);
   not       i9  (IQN, IQ);
   and       i10 (qin_and_set, IQN, SN);
   buf       i11 (Q, IQ);
   buf       i12 (QN, qin_and_set);

   checkrs   i13 (c_SH_dat_i, RN, SN);
   not       i14 (SE_not, SE);
   and       i15 (c_SH_D, c_SH_dat_i, SE_not);
   and       i16 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRLLX0 (CN, D, Q, QN, RN, SD, SE);

   input     CN, D, RN, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, d_clock_i,
                   d_dat_i, IQN, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i12 (SE_not, delay_SE);
   and       i13 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, RN, 1'b1, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_dat_i, RN, 1'b1);
   not       i12 (SE_not, SE);
   and       i13 (c_SH_D, c_SH_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRLLX1 (CN, D, Q, QN, RN, SD, SE);

   input     CN, D, RN, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, d_clock_i,
                   d_dat_i, IQN, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i12 (SE_not, delay_SE);
   and       i13 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, RN, 1'b1, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_dat_i, RN, 1'b1);
   not       i12 (SE_not, SE);
   and       i13 (c_SH_D, c_SH_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRLLX2 (CN, D, Q, QN, RN, SD, SE);

   input     CN, D, RN, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, d_clock_i,
                   d_dat_i, IQN, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i12 (SE_not, delay_SE);
   and       i13 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, RN, 1'b1, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_dat_i, RN, 1'b1);
   not       i12 (SE_not, SE);
   and       i13 (c_SH_D, c_SH_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFRLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFRLLX4 (CN, D, Q, QN, RN, SD, SE);

   input     CN, D, RN, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_RN, d_clock_i,
                   d_dat_i, IQN, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, delay_RN, 1'b1, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i12 (SE_not, delay_SE);
   and       i13 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i1  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, RN, 1'b1, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_dat_i, RN, 1'b1);
   not       i12 (SE_not, SE);
   and       i13 (c_SH_D, c_SH_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFSQLLX0 (CN, D, Q, SD, SE, SN);

   input     CN, D, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_SN, d_clock_i,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, 1'b1, SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, 1'b1, SN);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFSQLLX1 (CN, D, Q, SD, SE, SN);

   input     CN, D, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_SN, d_clock_i,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, 1'b1, SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, 1'b1, SN);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFSQLLX2 (CN, D, Q, SD, SE, SN);

   input     CN, D, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_SN, d_clock_i,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, 1'b1, SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, 1'b1, SN);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge single Q D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFSQLLX4 (CN, D, Q, SD, SE, SN);

   input     CN, D, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_SN, d_clock_i,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, 1'b1, SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, 1'b1, SN);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFSLLX0 (CN, D, Q, QN, SD, SE, SN);

   input     CN, D, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_SN, d_clock_i,
                   d_dat_i, IQN, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i12 (SE_not, delay_SE);
   and       i13 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, 1'b1, SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_dat_i, 1'b1, SN);
   not       i12 (SE_not, SE);
   and       i13 (c_SH_D, c_SH_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFSLLX1 (CN, D, Q, QN, SD, SE, SN);

   input     CN, D, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_SN, d_clock_i,
                   d_dat_i, IQN, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i12 (SE_not, delay_SE);
   and       i13 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, 1'b1, SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_dat_i, 1'b1, SN);
   not       i12 (SE_not, SE);
   and       i13 (c_SH_D, c_SH_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFSLLX2 (CN, D, Q, QN, SD, SE, SN);

   input     CN, D, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_SN, d_clock_i,
                   d_dat_i, IQN, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i12 (SE_not, delay_SE);
   and       i13 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, 1'b1, SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_dat_i, 1'b1, SN);
   not       i12 (SE_not, SE);
   and       i13 (c_SH_D, c_SH_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFSLLX4 (CN, D, Q, QN, SD, SE, SN);

   input     CN, D, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, delay_SN, d_clock_i,
                   d_dat_i, IQN, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, d_clock_i, 1'b1, delay_SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i12 (SE_not, delay_SE);
   and       i13 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_CN);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, clock_i, 1'b1, SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   buf       i9  (Q, IQ);
   buf       i10 (QN, IQN);

   checkrs   i11 (c_SH_dat_i, 1'b1, SN);
   not       i12 (SE_not, SE);
   and       i13 (c_SH_D, c_SH_dat_i, SE_not);
   and       i14 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, negedge CN, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFLLX0 (CN, D, Q, QN, SD, SE);

   input     CN, D, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, d_clock_i, d_dat_i,
                   IQN, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   not       i10 (c_SH_D, delay_SE);
   buf       i11 (c_SH_SD, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   not       i10 (c_SH_D, SE);
   buf       i11 (c_SH_SD, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFLLX1 (CN, D, Q, QN, SD, SE);

   input     CN, D, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, d_clock_i, d_dat_i,
                   IQN, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   not       i10 (c_SH_D, delay_SE);
   buf       i11 (c_SH_SD, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   not       i10 (c_SH_D, SE);
   buf       i11 (c_SH_SD, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFLLX2 (CN, D, Q, QN, SD, SE);

   input     CN, D, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, d_clock_i, d_dat_i,
                   IQN, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   not       i10 (c_SH_D, delay_SE);
   buf       i11 (c_SH_SD, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   not       i10 (c_SH_D, SE);
   buf       i11 (c_SH_SD, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFFLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : negedge D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFFLLX4 (CN, D, Q, QN, SD, SE);

   input     CN, D, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_CN, delay_D, delay_SD, delay_SE, d_clock_i, d_dat_i,
                   IQN, c_SH_D, c_SH_SD;

   not       i0  (d_clock_i, delay_CN);
   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, d_clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   not       i10 (c_SH_D, delay_SE);
   buf       i11 (c_SH_SD, delay_SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_CN, delay_D);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02,
                   NOTIFY_REG,,, delay_CN, delay_SD);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_CN,
                   delay_SE);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, clock_i, dat_i, IQN, c_SH_D, c_SH_SD;

   not       i0  (clock_i, CN);
   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, clock_i, 1'b1, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   not       i10 (c_SH_D, SE);
   buf       i11 (c_SH_SD, SE);

// timing section:
   specify

      (negedge CN => (Q -: Q)) = (0.02, 0.02);

      (negedge CN => (QN -: QN)) = (0.02, 0.02);

      $setuphold (negedge CN &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (negedge CN, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (negedge CN, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge CN, 0.02, 0, NOTIFY_REG);
      $width (negedge CN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRQLLX0 (C, D, Q, SD, SE);

   input     C, D, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, d_dat_i, c_SH_D,
                   c_SH_SD;

   u_mx2     i4  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i5  (IQ, d_dat_i, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i6  (Q, IQ);

   not       i7  (c_SH_D, delay_SE);
   buf       i8  (c_SH_SD, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_D, c_SH_SD;

   u_mx2     i4  (dat_i, D, SD, SE);
   u1_fd5    i5  (IQ, dat_i, C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i6  (Q, IQ);

   not       i7  (c_SH_D, SE);
   buf       i8  (c_SH_SD, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRQLLX1 (C, D, Q, SD, SE);

   input     C, D, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, d_dat_i, c_SH_D,
                   c_SH_SD;

   u_mx2     i4  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i5  (IQ, d_dat_i, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i6  (Q, IQ);

   not       i7  (c_SH_D, delay_SE);
   buf       i8  (c_SH_SD, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_D, c_SH_SD;

   u_mx2     i4  (dat_i, D, SD, SE);
   u1_fd5    i5  (IQ, dat_i, C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i6  (Q, IQ);

   not       i7  (c_SH_D, SE);
   buf       i8  (c_SH_SD, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRQLLX2 (C, D, Q, SD, SE);

   input     C, D, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, d_dat_i, c_SH_D,
                   c_SH_SD;

   u_mx2     i4  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i5  (IQ, d_dat_i, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i6  (Q, IQ);

   not       i7  (c_SH_D, delay_SE);
   buf       i8  (c_SH_SD, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_D, c_SH_SD;

   u_mx2     i4  (dat_i, D, SD, SE);
   u1_fd5    i5  (IQ, dat_i, C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i6  (Q, IQ);

   not       i7  (c_SH_D, SE);
   buf       i8  (c_SH_SD, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRQLLX4 (C, D, Q, SD, SE);

   input     C, D, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, d_dat_i, c_SH_D,
                   c_SH_SD;

   u_mx2     i4  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i5  (IQ, d_dat_i, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i6  (Q, IQ);

   not       i7  (c_SH_D, delay_SE);
   buf       i8  (c_SH_SD, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_D, c_SH_SD;

   u_mx2     i4  (dat_i, D, SD, SE);
   u1_fd5    i5  (IQ, dat_i, C, 1'b1, 1'b1, NOTIFY_REG);
   buf       i6  (Q, IQ);

   not       i7  (c_SH_D, SE);
   buf       i8  (c_SH_SD, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRQLLX0 (C, D, Q, RN, SD, SE);

   input     C, D, RN, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, d_dat_i,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i9  (SE_not, delay_SE);
   and       i10 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, RN, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_dat_i, RN, 1'b1);
   not       i9  (SE_not, SE);
   and       i10 (c_SH_D, c_SH_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRQLLX1 (C, D, Q, RN, SD, SE);

   input     C, D, RN, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, d_dat_i,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i9  (SE_not, delay_SE);
   and       i10 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, RN, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_dat_i, RN, 1'b1);
   not       i9  (SE_not, SE);
   and       i10 (c_SH_D, c_SH_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRQLLX2 (C, D, Q, RN, SD, SE);

   input     C, D, RN, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, d_dat_i,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i9  (SE_not, delay_SE);
   and       i10 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, RN, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_dat_i, RN, 1'b1);
   not       i9  (SE_not, SE);
   and       i10 (c_SH_D, c_SH_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRQLLX4 (C, D, Q, RN, SD, SE);

   input     C, D, RN, SD, SE;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, d_dat_i,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i9  (SE_not, delay_SE);
   and       i10 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, RN, 1'b1, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_dat_i, RN, 1'b1);
   not       i9  (SE_not, SE);
   and       i10 (c_SH_D, c_SH_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset, Set and
//                      Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRSQLLX0 (C, D, Q, RN, SD, SE, SN);

   input     C, D, RN, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, C, RN, SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, RN, SN);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset, Set and
//                      Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRSQLLX1 (C, D, Q, RN, SD, SE, SN);

   input     C, D, RN, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, C, RN, SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, RN, SN);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset, Set and
//                      Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRSQLLX2 (C, D, Q, RN, SD, SE, SN);

   input     C, D, RN, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, C, RN, SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, RN, SN);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Reset, Set and
//                      Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRSQLLX4 (C, D, Q, RN, SD, SE, SN);

   input     C, D, RN, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_dat_i, c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i10 (SE_not, delay_SE);
   and       i11 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, C, RN, SN, NOTIFY_REG);
   buf       i8  (Q, IQ);

   checkrs   i9  (c_SH_dat_i, RN, SN);
   not       i10 (SE_not, SE);
   and       i11 (c_SH_D, c_SH_dat_i, SE_not);
   and       i12 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset, Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRSLLX0 (C, D, Q, QN, RN, SD, SE, SN);

   input     C, D, RN, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_dat_i, IQN, qin_and_set, c_SH_d_dat_i, SE_not, c_SH_D,
                   c_SH_SD;

   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   and       i9  (qin_and_set, IQN, delay_SN);
   buf       i10 (Q, IQ);
   buf       i11 (QN, qin_and_set);

   checkrs   i12 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i13 (SE_not, delay_SE);
   and       i14 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i15 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, qin_and_set, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, C, RN, SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   and       i9  (qin_and_set, IQN, SN);
   buf       i10 (Q, IQ);
   buf       i11 (QN, qin_and_set);

   checkrs   i12 (c_SH_dat_i, RN, SN);
   not       i13 (SE_not, SE);
   and       i14 (c_SH_D, c_SH_dat_i, SE_not);
   and       i15 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset, Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRSLLX1 (C, D, Q, QN, RN, SD, SE, SN);

   input     C, D, RN, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_dat_i, IQN, qin_and_set, c_SH_d_dat_i, SE_not, c_SH_D,
                   c_SH_SD;

   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   and       i9  (qin_and_set, IQN, delay_SN);
   buf       i10 (Q, IQ);
   buf       i11 (QN, qin_and_set);

   checkrs   i12 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i13 (SE_not, delay_SE);
   and       i14 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i15 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, qin_and_set, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, C, RN, SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   and       i9  (qin_and_set, IQN, SN);
   buf       i10 (Q, IQ);
   buf       i11 (QN, qin_and_set);

   checkrs   i12 (c_SH_dat_i, RN, SN);
   not       i13 (SE_not, SE);
   and       i14 (c_SH_D, c_SH_dat_i, SE_not);
   and       i15 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset, Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRSLLX2 (C, D, Q, QN, RN, SD, SE, SN);

   input     C, D, RN, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_dat_i, IQN, qin_and_set, c_SH_d_dat_i, SE_not, c_SH_D,
                   c_SH_SD;

   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   and       i9  (qin_and_set, IQN, delay_SN);
   buf       i10 (Q, IQ);
   buf       i11 (QN, qin_and_set);

   checkrs   i12 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i13 (SE_not, delay_SE);
   and       i14 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i15 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, qin_and_set, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, C, RN, SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   and       i9  (qin_and_set, IQN, SN);
   buf       i10 (Q, IQ);
   buf       i11 (QN, qin_and_set);

   checkrs   i12 (c_SH_dat_i, RN, SN);
   not       i13 (SE_not, SE);
   and       i14 (c_SH_D, c_SH_dat_i, SE_not);
   and       i15 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset, Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRSLLX4 (C, D, Q, QN, RN, SD, SE, SN);

   input     C, D, RN, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, delay_SN,
                   d_dat_i, IQN, qin_and_set, c_SH_d_dat_i, SE_not, c_SH_D,
                   c_SH_SD;

   u_mx2     i6  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i7  (IQ, d_dat_i, delay_C, delay_RN, delay_SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   and       i9  (qin_and_set, IQN, delay_SN);
   buf       i10 (Q, IQ);
   buf       i11 (QN, qin_and_set);

   checkrs   i12 (c_SH_d_dat_i, delay_RN, delay_SN);
   not       i13 (SE_not, delay_SE);
   and       i14 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i15 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_SN);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, qin_and_set, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i6  (dat_i, D, SD, SE);
   u1_fd5    i7  (IQ, dat_i, C, RN, SN, NOTIFY_REG);
   not       i8  (IQN, IQ);
   and       i9  (qin_and_set, IQN, SN);
   buf       i10 (Q, IQ);
   buf       i11 (QN, qin_and_set);

   checkrs   i12 (c_SH_dat_i, RN, SN);
   not       i13 (SE_not, SE);
   and       i14 (c_SH_D, c_SH_dat_i, SE_not);
   and       i15 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (RN +=> Q) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (SN +=> QN) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge RN, posedge SN, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRLLX0 (C, D, Q, QN, RN, SD, SE);

   input     C, D, RN, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, d_dat_i, IQN,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, RN, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_dat_i, RN, 1'b1);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRLLX1 (C, D, Q, QN, RN, SD, SE);

   input     C, D, RN, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, d_dat_i, IQN,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, RN, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_dat_i, RN, 1'b1);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRLLX2 (C, D, Q, QN, RN, SD, SE);

   input     C, D, RN, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, d_dat_i, IQN,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, RN, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_dat_i, RN, 1'b1);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRRLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Reset and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRRLLX4 (C, D, Q, QN, RN, SD, SE);

   input     C, D, RN, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_RN, d_dat_i, IQN,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, delay_RN, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_d_dat_i, delay_RN, 1'b1);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_RN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, RN, 1'b1, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_dat_i, RN, 1'b1);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);
      (negedge RN => (Q +: RN)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);
      (negedge RN => (QN -: RN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge RN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge RN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRSQLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRSQLLX0 (C, D, Q, SD, SE, SN);

   input     C, D, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_SN, d_dat_i,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i9  (SE_not, delay_SE);
   and       i10 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, 1'b1, SN, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_dat_i, 1'b1, SN);
   not       i9  (SE_not, SE);
   and       i10 (c_SH_D, c_SH_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRSQLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRSQLLX1 (C, D, Q, SD, SE, SN);

   input     C, D, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_SN, d_dat_i,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i9  (SE_not, delay_SE);
   and       i10 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, 1'b1, SN, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_dat_i, 1'b1, SN);
   not       i9  (SE_not, SE);
   and       i10 (c_SH_D, c_SH_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRSQLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRSQLLX2 (C, D, Q, SD, SE, SN);

   input     C, D, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_SN, d_dat_i,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i9  (SE_not, delay_SE);
   and       i10 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, 1'b1, SN, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_dat_i, 1'b1, SN);
   not       i9  (SE_not, SE);
   and       i10 (c_SH_D, c_SH_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRSQLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge single Q D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRSQLLX4 (C, D, Q, SD, SE, SN);

   input     C, D, SD, SE, SN;
   output    Q;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_SN, d_dat_i,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i9  (SE_not, delay_SE);
   and       i10 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, 1'b1, SN, NOTIFY_REG);
   buf       i7  (Q, IQ);

   checkrs   i8  (c_SH_dat_i, 1'b1, SN);
   not       i9  (SE_not, SE);
   and       i10 (c_SH_D, c_SH_dat_i, SE_not);
   and       i11 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRSLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRSLLX0 (C, D, Q, QN, SD, SE, SN);

   input     C, D, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_SN, d_dat_i, IQN,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, 1'b1, SN, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_dat_i, 1'b1, SN);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRSLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRSLLX1 (C, D, Q, QN, SD, SE, SN);

   input     C, D, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_SN, d_dat_i, IQN,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, 1'b1, SN, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_dat_i, 1'b1, SN);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRSLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRSLLX2 (C, D, Q, QN, SD, SE, SN);

   input     C, D, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_SN, d_dat_i, IQN,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, 1'b1, SN, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_dat_i, 1'b1, SN);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRSLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Set and Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRSLLX4 (C, D, Q, QN, SD, SE, SN);

   input     C, D, SD, SE, SN;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, delay_SN, d_dat_i, IQN,
                   c_SH_d_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i6  (IQ, d_dat_i, delay_C, 1'b1, delay_SN, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_d_dat_i, 1'b1, delay_SN);
   not       i11 (SE_not, delay_SE);
   and       i12 (c_SH_D, c_SH_d_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_d_dat_i, delay_SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG,,, delay_SN,
                   delay_C);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_dat_i, SE_not, c_SH_D, c_SH_SD;

   u_mx2     i5  (dat_i, D, SD, SE);
   u1_fd5    i6  (IQ, dat_i, C, 1'b1, SN, NOTIFY_REG);
   not       i7  (IQN, IQ);
   buf       i8  (Q, IQ);
   buf       i9  (QN, IQN);

   checkrs   i10 (c_SH_dat_i, 1'b1, SN);
   not       i11 (SE_not, SE);
   and       i12 (c_SH_D, c_SH_dat_i, SE_not);
   and       i13 (c_SH_SD, c_SH_dat_i, SE);

// timing section:
   specify

      (negedge SN => (Q -: SN)) = (0.02, 0.02);
      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (negedge SN => (QN +: SN)) = (0.02, 0.02);
      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $recrem (posedge SN, posedge C, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge SN, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRLLX0 (C, D, Q, QN, SD, SE);

   input     C, D, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, d_dat_i, IQN, c_SH_D,
                   c_SH_SD;

   u_mx2     i4  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i5  (IQ, d_dat_i, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   not       i6  (IQN, IQ);
   buf       i7  (Q, IQ);
   buf       i8  (QN, IQN);

   not       i9  (c_SH_D, delay_SE);
   buf       i10 (c_SH_SD, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_D, c_SH_SD;

   u_mx2     i4  (dat_i, D, SD, SE);
   u1_fd5    i5  (IQ, dat_i, C, 1'b1, 1'b1, NOTIFY_REG);
   not       i6  (IQN, IQ);
   buf       i7  (Q, IQ);
   buf       i8  (QN, IQN);

   not       i9  (c_SH_D, SE);
   buf       i10 (c_SH_SD, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRLLX1 (C, D, Q, QN, SD, SE);

   input     C, D, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, d_dat_i, IQN, c_SH_D,
                   c_SH_SD;

   u_mx2     i4  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i5  (IQ, d_dat_i, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   not       i6  (IQN, IQ);
   buf       i7  (Q, IQ);
   buf       i8  (QN, IQN);

   not       i9  (c_SH_D, delay_SE);
   buf       i10 (c_SH_SD, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_D, c_SH_SD;

   u_mx2     i4  (dat_i, D, SD, SE);
   u1_fd5    i5  (IQ, dat_i, C, 1'b1, 1'b1, NOTIFY_REG);
   not       i6  (IQN, IQ);
   buf       i7  (Q, IQ);
   buf       i8  (QN, IQN);

   not       i9  (c_SH_D, SE);
   buf       i10 (c_SH_SD, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRLLX2 (C, D, Q, QN, SD, SE);

   input     C, D, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, d_dat_i, IQN, c_SH_D,
                   c_SH_SD;

   u_mx2     i4  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i5  (IQ, d_dat_i, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   not       i6  (IQN, IQ);
   buf       i7  (Q, IQ);
   buf       i8  (QN, IQN);

   not       i9  (c_SH_D, delay_SE);
   buf       i10 (c_SH_SD, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_D, c_SH_SD;

   u_mx2     i4  (dat_i, D, SD, SE);
   u1_fd5    i5  (IQ, dat_i, C, 1'b1, 1'b1, NOTIFY_REG);
   not       i6  (IQN, IQ);
   buf       i7  (Q, IQ);
   buf       i8  (QN, IQN);

   not       i9  (c_SH_D, SE);
   buf       i10 (c_SH_SD, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SDFRLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : posedge D-Flip-Flop with Scan
//   last modified by : XLICDD generated
//****************************************************************************

module SDFRLLX4 (C, D, Q, QN, SD, SE);

   input     C, D, SD, SE;
   output    Q, QN;

`ifdef NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, delay_C, delay_D, delay_SD, delay_SE, d_dat_i, IQN, c_SH_D,
                   c_SH_SD;

   u_mx2     i4  (d_dat_i, delay_D, delay_SD, delay_SE);
   u1_fd5    i5  (IQ, d_dat_i, delay_C, 1'b1, 1'b1, NOTIFY_REG);
   not       i6  (IQN, IQ);
   buf       i7  (Q, IQ);
   buf       i8  (QN, IQN);

   not       i9  (c_SH_D, delay_SE);
   buf       i10 (c_SH_SD, delay_SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_D);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG,,,
                   delay_C, delay_SD);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG,,, delay_C,
                   delay_SE);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`else   // NEG_TCHK

   reg       NOTIFY_REG;
   wire	     IQ, dat_i, IQN, c_SH_D, c_SH_SD;

   u_mx2     i4  (dat_i, D, SD, SE);
   u1_fd5    i5  (IQ, dat_i, C, 1'b1, 1'b1, NOTIFY_REG);
   not       i6  (IQN, IQ);
   buf       i7  (Q, IQ);
   buf       i8  (QN, IQN);

   not       i9  (c_SH_D, SE);
   buf       i10 (c_SH_SD, SE);

// timing section:
   specify

      (posedge C => (Q -: Q)) = (0.02, 0.02);

      (posedge C => (QN -: QN)) = (0.02, 0.02);

      $setuphold (posedge C &&& c_SH_D, posedge D, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_D, negedge D, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C &&& c_SH_SD, posedge SD, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C &&& c_SH_SD, negedge SD, 0.02, 0.02, NOTIFY_REG);

      $setuphold (posedge C, posedge SE, 0.02, 0.02, NOTIFY_REG);
      $setuphold (posedge C, negedge SE, 0.02, 0.02, NOTIFY_REG);

      $width (posedge C, 0.02, 0, NOTIFY_REG);
      $width (negedge C, 0.02, 0, NOTIFY_REG);

   endspecify

`endif   // NEG_TCHK
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : STELLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : ESD/EMC optimized buffer with CMOS Schmitt Trigger
//                      input
//   last modified by : XLICDD generated
//****************************************************************************

module STELLX1 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : STELLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : ESD/EMC optimized buffer with CMOS Schmitt Trigger
//                      input
//   last modified by : XLICDD generated
//****************************************************************************

module STELLX2 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : STELLX3
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : ESD/EMC optimized buffer with CMOS Schmitt Trigger
//                      input
//   last modified by : XLICDD generated
//****************************************************************************

module STELLX3 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : STELLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : ESD/EMC optimized buffer with CMOS Schmitt Trigger
//                      input
//   last modified by : XLICDD generated
//****************************************************************************

module STELLX4 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : STLLX0
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer with CMOS Schmitt Trigger input
//   last modified by : XLICDD generated
//****************************************************************************

module STLLX0 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : STLLX1
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer with CMOS Schmitt Trigger input
//   last modified by : XLICDD generated
//****************************************************************************

module STLLX1 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : STLLX2
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer with CMOS Schmitt Trigger input
//   last modified by : XLICDD generated
//****************************************************************************

module STLLX2 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : STLLX3
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer with CMOS Schmitt Trigger input
//   last modified by : XLICDD generated
//****************************************************************************

module STLLX3 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : STLLX4
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Buffer with CMOS Schmitt Trigger input
//   last modified by : XLICDD generated
//****************************************************************************

module STLLX4 (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A +=> Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
//*****************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : SIGNALHOLDLL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Leakage current compensator
//   last modified by : XLICDD generated
//*****************************************************************

module SIGNALHOLDLL (SIG);

   inout SIG;
   wire n1;

// logic section  
  `ifdef DISPLAY_HOLD 
  
        buf i1 (n1, SIG);
        buf (pull1, pull0) i2 (SIG, n1);

     initial
     begin
      #0 $display("  > Warning: compiler directive DISPLAY_HOLD is set in cell"
                               );
         $display("  > %m");
         $display("  > SIGNALHOLD cell model is switched to provide logic levels"
                  );
         $display("  >  - danger of reading not really driven values ");   
                   
         $display("  >  - undriven bus states are not detectable now ");   
                   
         $display("  >");    
     end

  `else
// - no logic behaviour modelled for the electrical function of 
//   signalhold cell 
// - signalhold cell compensates leackage current only in case of
//   undriven node/bus and system stop
// - signalhold cell holds the bus bit on "0" or "1", but does not 
//   drive it, this is not in every case the same as the last valid 
//   logic value considering the desired application meaning
// - undriven bus bit should not be used as sequential element
// - reading from undriven bus_bit ("Z") has to provide ("X") in the
//   related block to detect such cases in simulation
//
// Use the compiler directive command
//   `define  DISPLAY_HOLD yes
// to enable the logic pullup/down behaviour of the Leakage current 
// compensator cell SIGNALHOLD*
     buf i3 (n1, SIG);
     bufif1 i4 (SIG, n1, 1'b0); /* always inactive */
  `endif
 
// no timing modelled for signalhold cell
// no backannotation possible for signalhold cell 
    

// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LOGIC0LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Constant logic 0
//   last modified by : XLICDD generated
//****************************************************************************

module LOGIC0LL (Q);

   output    Q;

// Function Q: 0
   buf       i0   (Q, 1'b0);

// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : LOGIC1LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Constant logic 1
//   last modified by : XLICDD generated
//****************************************************************************

module LOGIC1LL (Q);

   output    Q;

// Function Q: 1
   buf       i0   (Q, 1'b1);

// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MPROBELL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Micro probe cell for direct wire access
//   last modified by : XLICDD generated
//****************************************************************************

module MPROBELL (A);

   input     A;

// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : MPROBEBULLX8
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Microprobe cell for buffered signal output (signal
//                      decoupling)
//   last modified by : XLICDD generated
//****************************************************************************

module MPROBEBULLX8 (A);

   input     A;

// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FEED1LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell
//   last modified by : XLICDD generated
//****************************************************************************

module FEED1LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FEED2LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell
//   last modified by : XLICDD generated
//****************************************************************************

module FEED2LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FEED3LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell
//   last modified by : XLICDD generated
//****************************************************************************

module FEED3LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FEED5LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell
//   last modified by : XLICDD generated
//****************************************************************************

module FEED5LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FEED7LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell
//   last modified by : XLICDD generated
//****************************************************************************

module FEED7LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FEED10LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell
//   last modified by : XLICDD generated
//****************************************************************************

module FEED10LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FEED15LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell
//   last modified by : XLICDD generated
//****************************************************************************

module FEED15LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : FEED25LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell
//   last modified by : XLICDD generated
//****************************************************************************

module FEED25LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP3LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP3LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP5LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP5LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP7LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP7LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP10LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP10LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP15LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP15LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP25LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP25LL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP5LPLL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized, low power (low gate leakage)
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP5LPLL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP7LPLL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized, low power (low gate leakage)
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP7LPLL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP10LPLL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized, low power (low gate leakage)
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP10LPLL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP15LPLL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized, low power (low gate leakage)
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP15LPLL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : DECAP25LPLL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Filler cell with buffering capacitance, ESD
//                      optimized, low power (low gate leakage)
//   last modified by : XLICDD generated
//****************************************************************************

module DECAP25LPLL ();


// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ANTENNACELL2LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : protection cell against antenna effects (net
//                      charge) at manufacture, PIMP diode in NWELL
//   last modified by : XLICDD generated
//****************************************************************************

module ANTENNACELL2LL (A);

   input     A;

// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ANTENNACELLN2LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : protection cell against antenna effects (net
//                      charge) at manufacture, NIMP diode in substrate
//   last modified by : XLICDD generated
//****************************************************************************

module ANTENNACELLN2LL (A);

   input     A;

// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : ANTENNACELLNP2LL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : protection cell against antenna effects (net
//                      charge) at manufacture, PIMP diode in NWELL, NIMP
//                 diode in substrate
//   last modified by : XLICDD generated
//****************************************************************************

module ANTENNACELLNP2LL (A);

   input     A;

// timing section:
   specify

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine

`timescale 1ns/10ps
`celldefine
`suppress_faults
`enable_portfaults
//****************************************************************************
//   technology       : 0.18um HV SOI CMOS
//   module name      : CLKVBUFLL
//   version          : 2.0.0, Wed Aug 10 12:47:20 2016
//   cell_description : Virtual Clock Tree Buffer
//   last modified by : XLICDD generated
//****************************************************************************

module CLKVBUFLL (A, Q);

   input     A;
   output    Q;

// Function Q: A
   buf       i0  (Q, A);

// timing section:
   specify

      (A => Q) = (0.02, 0.02);

   endspecify
endmodule
//****************************************************************************
`nosuppress_faults
`disable_portfaults
`endcelldefine
