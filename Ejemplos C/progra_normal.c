#include <stdio.h>

int main() {
    int a = 12;
    int b = 5;
    int resultado;

    // Realiza la suma
    resultado = a + b;

    // Muestra el resultado por consola
    printf("El resultado de la suma es: %d\n", resultado);

    return 0;
}
